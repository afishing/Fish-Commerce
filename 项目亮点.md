# Fish-Commerce — 基于 Spring Cloud Alibaba 的水产品电商平台

**技术栈：** Java 17 / Spring Boot 3 / Spring Cloud 2023 / Vue 3 / MySQL / Redis / RabbitMQ / Docker / Nginx

## 项目亮点

- **微服务架构：** 拆分为 8 个业务服务 + 1 个 API 网关，通过 Nacos 实现服务注册发现，OpenFeign 服务间调用，Sentinel 熔断降级
- **分库分治：** 按业务领域划分 8 个独立 MySQL 数据库（用户/商品/订单/购物车/支付/内容/营销/视频），降低耦合
- **AI 智能客服：** 集成 DeepSeek 大模型，基于 SSE 实现流式响应，支持多轮上下文对话
- **Redis 缓存：** 热门商品列表缓存，降低数据库压力，提升接口响应速度
- **异步解耦：** 使用 RabbitMQ 处理订单超时自动取消、支付回调通知等异步场景
- **Docker 一键部署：** docker-compose 编排 14 个容器（4 中间件 + 9 服务 + Nginx），支持一键构建启动
- **Nginx 反向代理：** 前端静态资源托管 + API 网关转发 + SSE 流式支持
- **全链路监控：** 基于 Spring Boot Admin 实现 JVM/内存/线程/CPU 实时监控
- **前后端分离：** Vue 3 + Element Plus 构建用户端和管理后台双端界面

**个人职责：** 独立完成系统设计、后端开发、前端开发、数据库设计及 Docker 容器化部署全流程

---

## 简短版

> 基于 Spring Cloud Alibaba 的微服务电商平台，拆分 8 个服务 + 8 个独立数据库。集成 DeepSeek AI 智能客服（SSE 流式响应）、Redis 热商品缓存、RabbitMQ 异步消息、Docker Compose 14 容器一键部署、Spring Boot Admin 全链路监控。独立完成全栈开发与容器化部署。

---

## 分布式架构实现与理论分析

### 一、Nacos 服务注册与发现

#### 1.1 架构角色

本项目采用 Nacos 2.4.3 作为服务注册中心，9 个微服务（user/product/order/cart/pay/content/ai/monitor/gateway）启动时向 Nacos 注册实例，Gateway 通过 Nacos 动态获取服务列表进行路由转发。

#### 1.2 一致性模型

Nacos 注册中心采用 **AP 模式**（临时实例），基于 **Distro 协议** 实现最终一致性。根据 CAP 定理：

$$
\text{分布式系统最多同时满足 } C、A、P \text{ 中的两个}
$$

其中：
- **C（Consistency）**：一致性，所有节点同一时刻数据相同
- **A（Availability）**：可用性，每个请求都能得到响应
- **P（Partition tolerance）**：分区容错性，网络分区时系统仍能运行

Nacos 选择 **AP**，在网络分区时牺牲强一致性保证服务可用，适合电商场景下服务发现的高可用需求。

#### 1.3 心跳检测与健康判定

服务实例定期向 Nacos 发送心跳，健康状态判定函数为：

$$
H(t) = \begin{cases} 1 & \text{if } t_{\text{now}} - t_{\text{last\_heartbeat}} \leq T_{\text{timeout}} \\ 0 & \text{if } t_{\text{now}} - t_{\text{last\_heartbeat}} > T_{\text{timeout}} \end{cases}
$$

其中 $T_{\text{timeout}} = 15s$（默认），若连续 $30s$ 无心跳则剔除实例：

$$
\text{Remove}(i) = \begin{cases} \text{true} & \text{if } t_{\text{now}} - t_{\text{last\_heartbeat}} > 2T_{\text{timeout}} \\ \text{false} & \text{otherwise} \end{cases}
$$

系统整体可用性为所有服务实例可用性的乘积：

$$
A_{\text{system}} = \prod_{i=1}^{n} A_i
$$

当单个服务部署 $k$ 个实例时，该服务可用性为：

$$
A_{\text{service}} = 1 - (1 - A_{\text{single}})^k
$$

例如单实例可用性 $A_{\text{single}} = 0.99$，部署 3 个实例后：

$$
A_{\text{service}} = 1 - (1 - 0.99)^3 = 1 - 10^{-6} = 0.999999
$$

---

### 二、OpenFeign 声明式服务调用

#### 2.1 架构角色

本项目中 Gateway 通过 OpenFeign 实现服务间的声明式 HTTP 调用，如：订单服务调用商品服务扣减库存、购物车服务调用商品服务查询价格。

#### 2.2 负载均衡算法

OpenFeign 底层集成 Spring Cloud LoadBalancer，默认采用 **轮询（Round Robin）** 策略。对于 $n$ 个服务实例，第 $k$ 次请求路由到的实例索引为：

$$
\text{index}(k) = k \mod n
$$

加权轮询（Weighted Round Robin）中，实例 $i$ 被选中的概率为：

$$
P(i) = \frac{w_i}{\sum_{j=1}^{n} w_j}
$$

其中 $w_i$ 为实例 $i$ 的权重。

#### 2.3 调用链延迟分析

在微服务调用链中，总延迟为各环节延迟之和：

$$
T_{\text{total}} = T_{\text{gateway}} + T_{\text{feign}} + T_{\text{service}} + T_{\text{db}}
$$

以「创建订单」为例，调用链为：

$$
\text{Gateway} \xrightarrow{\text{路由}} \text{OrderService} \xrightarrow{\text{Feign}} \text{ProductService} \xrightarrow{\text{扣库存}} \text{MySQL}
$$

设各环节延迟服从独立分布，总延迟期望为：

$$
E[T_{\text{total}}] = E[T_{\text{gateway}}] + E[T_{\text{feign}}] + E[T_{\text{service}}] + E[T_{\text{db}}]
$$

#### 2.4 重试机制

OpenFeign 的重试决策函数为：

$$
\text{Retry}(r) = \begin{cases} \text{重试} & \text{if } r < R_{\max} \text{ and } \text{isRetryable}(e) \\ \text{失败} & \text{otherwise} \end{cases}
$$

其中 $R_{\max}$ 为最大重试次数。经过 $r$ 次重试后请求成功的概率为：

$$
P_{\text{success}} = 1 - (1 - p)^{r+1}
$$

其中 $p$ 为单次请求成功率。

---

### 三、Sentinel 熔断降级

#### 3.1 架构角色

本项目在 Gateway 和各微服务中集成 Sentinel，实现接口级别的流量控制和熔断降级，防止级联故障。

#### 3.2 滑动窗口限流

Sentinel 采用**滑动时间窗口**统计 QPS。将时间窗口 $W$ 划分为 $n$ 个 bucket，每个 bucket 时长 $\Delta t = W / n$：

$$
\text{QPS}(t) = \frac{\sum_{i=1}^{n} C_i(t)}{W}
$$

其中 $C_i(t)$ 为第 $i$ 个 bucket 在时刻 $t$ 的请求计数。限流判定：

$$
\text{Allow}(t) = \begin{cases} \text{通过} & \text{if } \text{QPS}(t) \leq Q_{\text{threshold}} \\ \text{拒绝} & \text{if } \text{QPS}(t) > Q_{\text{threshold}} \end{cases}
$$

#### 3.3 令牌桶算法

Sentinel 的流控模式之一为**令牌桶（Token Bucket）**，令牌以速率 $r$ 生成，桶容量为 $B$：

$$
\text{tokens}(t) = \min\left(B, \ \text{tokens}(t_0) + r \cdot (t - t_0)\right)
$$

请求到达时消耗 1 个令牌，令牌不足则拒绝：

$$
\text{Allow} = \begin{cases} \text{通过}, \ \text{tokens} \leftarrow \text{tokens} - 1 & \text{if tokens} \geq 1 \\ \text{拒绝} & \text{if tokens} < 1 \end{cases}
$$

令牌桶允许突发流量（最多 $B$ 个请求瞬时通过），同时保证长期平均速率不超过 $r$。

#### 3.4 熔断器状态机

Sentinel 熔断器基于**有限状态自动机（FSM）**模型，状态集合 $S = \{\text{CLOSED}, \text{OPEN}, \text{HALF\_OPEN}\}$：

$$
\text{FSM}: S \times \Sigma \rightarrow S
$$

状态转移规则：

$$
\delta(\text{CLOSED}, \text{failRate} \geq \theta) = \text{OPEN}
$$

$$
\delta(\text{OPEN}, \text{timeout}) = \text{HALF\_OPEN}
$$

$$
\delta(\text{HALF\_OPEN}, \text{success}) = \text{CLOSED}
$$

$$
\delta(\text{HALF\_OPEN}, \text{fail}) = \text{OPEN}
$$

其中失败率的计算公式为：

$$
\text{failRate} = \frac{F}{F + S} \times 100\%
$$

$F$ 为失败次数，$S$ 为成功次数，$\theta$ 为熔断阈值（如 50%）。

#### 3.5 熔断对系统可用性的保护

无熔断时，级联故障概率随调用链深度指数增长：

$$
P_{\text{cascade}} = 1 - \prod_{i=1}^{d} (1 - p_i)
$$

其中 $d$ 为调用链深度，$p_i$ 为第 $i$ 层故障概率。当 $d = 4, p_i = 0.1$ 时：

$$
P_{\text{cascade}} = 1 - 0.9^4 = 0.3439
$$

引入 Sentinel 熔断后，故障被隔离在源头，系统降级但不崩溃：

$$
P_{\text{cascade}}^{\text{sentinel}} \approx p_1 \ll P_{\text{cascade}}
$$

---

### 四、Spring Cloud Gateway 统一网关

#### 4.1 架构角色

本项目 Gateway（端口 5000）作为所有流量的统一入口，承担路由转发、认证鉴权、限流熔断三大职责。配置了 12 条路由规则，覆盖全部 8 个业务服务。

#### 4.2 路由匹配

Gateway 基于 **Predicate 谓词链** 进行路由匹配，路由决策函数为：

$$
\text{Route}(r) = \text{first}\{R_i \ | \ \bigwedge_{j=1}^{m} P_{ij}(r) = \text{true}\}
$$

其中 $R_i$ 为第 $i$ 条路由规则，$P_{ij}$ 为该规则的第 $j$ 个谓词。本项目以 Path 谓词为主：

$$
P_{\text{path}}(r) = \text{match}(r.\text{path}, \text{pattern})
$$

例如 `/api/product/**` 匹配所有商品服务请求。

#### 4.3 过滤器链

Gateway 的请求处理采用**责任链模式**，过滤器按 Order 值排序执行：

$$
\text{Response} = F_n \circ F_{n-1} \circ \cdots \circ F_1(\text{Request})
$$

本项目的过滤器链为：

$$
\text{AuthFilter}(O=10) \rightarrow \text{StripPrefix} \rightarrow \text{LoadBalancer} \rightarrow \text{Service}
$$

其中 AuthFilter 实现白名单放行 + Token 验证：

$$
\text{Auth}(r) = \begin{cases} \text{放行} & \text{if } r.\text{path} \in \text{WhiteList} \\ \text{放行} & \text{if } \text{valid}(r.\text{token}) \\ \text{401} & \text{otherwise} \end{cases}
$$

#### 4.4 网关层限流

Gateway 结合 Sentinel 实现网关级限流，限流粒度为路由 ID。设限流阈值为 $Q$，请求到达率为 $\lambda$（泊松分布），则：

$$
P(\text{reject}) = P(X > Q) = 1 - \sum_{k=0}^{Q} \frac{\lambda^k e^{-\lambda}}{k!}
$$

当 $\lambda \leq Q$ 时，拒绝率极低；当 $\lambda \gg Q$ 时，多余请求被快速拒绝保护后端服务。

#### 4.5 整体请求链路

一个完整请求的处理流程可形式化为：

$$
\text{Client} \xrightarrow{\text{HTTP}} \text{Nginx} \xrightarrow{\text{proxy\_pass}} \text{Gateway} \xrightarrow{\text{route}} \text{Service}_i \xrightarrow{\text{SQL}} \text{MySQL}_j
$$

总延迟上界为：

$$
T_{\max} = T_{\text{nginx}} + T_{\text{gateway}} + T_{\text{sentinel}} + T_{\text{feign}} + T_{\text{service}} + T_{\text{db}}
$$

通过 Sentinel 设置超时阈值 $T_{\text{limit}}$，保证：

$$
T_{\text{response}} \leq T_{\text{limit}}, \quad \text{otherwise fallback}
$$

---

### 五、分布式协作总结

四个组件在本项目中的协作关系：

```
Nginx → Gateway（路由 + 鉴权 + Sentinel 限流）
         ↓
       Nacos（服务发现，获取实例列表）
         ↓
       OpenFeign（负载均衡调用目标服务）
         ↓
       Sentinel（熔断降级，防止级联故障）
```

系统整体吞吐量由 **短板服务** 决定：

$$
\text{Throughput}_{\text{system}} = \min_{i=1}^{n} \text{Throughput}_i
$$

通过 Sentinel 限流保护短板服务，Nacos 动态扩缩容提升瓶颈服务实例数，OpenFeign 负载均衡分散压力，Gateway 统一管控流量入口，四者协同实现了**高可用、可伸缩、可降级**的分布式电商系统。
