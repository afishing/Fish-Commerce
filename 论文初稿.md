# 基于Spring Cloud Alibaba的分布式电商系统设计与实现

## 摘要

随着互联网技术的快速发展和电子商务的普及，传统的单体架构已难以满足现代电商系统对高并发、高可用、可扩展性的需求。本文设计并实现了一个基于Spring Cloud Alibaba微服务架构的分布式电商系统——飞鱼商城。系统采用前后端分离的开发模式，后端基于Spring Boot 3.3.4和Spring Cloud Alibaba 2023.0.3.2构建微服务集群，前端采用Vue 3和Element Plus框架实现响应式用户界面。

系统主要包含用户服务、商品服务、订单服务、购物车服务、支付服务、视频服务、AI服务等核心模块，通过Nacos实现服务注册与配置管理，使用Spring Cloud Gateway实现API网关路由，采用Redis实现订单超时自动取消功能，集成Spring Boot Admin实现系统监控。系统实现了用户注册登录、商品浏览与搜索、购物车管理、订单创建与支付、库存预警、AI智能客服、后台管理等完整的电商业务流程。

经过系统测试，该系统具有良好的稳定性、可扩展性和用户体验，能够满足中小型电商平台的业务需求，对同类系统的开发具有一定的参考价值。

**关键词：** 微服务架构；Spring Cloud Alibaba；Nacos服务注册；OpenFeign；Sentinel熔断限流；Spring Cloud Gateway；分布式系统；电子商务

---

## 目录

1. 绪论
2. 相关技术介绍
3. 系统需求分析
4. 系统设计
5. 系统实现
6. 系统测试
7. 总结与展望
8. 参考文献
9. 致谢

---

## 第一章 绪论

### 1.1 研究背景与意义

#### 1.1.1 研究背景

近年来，随着移动互联网的普及和物流体系的完善，电子商务已成为人们日常生活中不可或缺的一部分。根据中国互联网络信息中心（CNNIC）发布的数据显示，截至2023年，我国网络购物用户规模已超过8亿，网络零售额连续多年保持两位数增长。电商行业的蓬勃发展对电商系统的技术架构提出了更高的要求。

传统的电商系统多采用单体架构，即所有的功能模块都部署在同一个应用程序中。这种架构在系统初期开发效率较高，但随着业务规模的扩大，单体架构逐渐暴露出以下问题：

（1）**可维护性差**：所有模块耦合在一起，修改一个功能可能影响其他功能，代码维护成本高。

（2）**扩展性差**：无法针对单个模块进行独立扩展，当某个模块成为性能瓶颈时，只能整体扩容，造成资源浪费。

（3）**可靠性差**：单体应用中任何一个模块出现故障，都可能导致整个系统崩溃。

（4）**技术选型受限**：整个系统必须使用相同的技术栈，难以针对不同业务场景选择最合适的技术。

为了解决上述问题，微服务架构应运而生。微服务架构将一个大型应用拆分为多个小型、独立的服务，每个服务专注于完成特定的业务功能，服务之间通过轻量级通信机制进行交互。这种架构具有独立部署、独立扩展、技术选型灵活等优点，已成为构建大型分布式系统的主流架构模式。

#### 1.1.2 研究意义

本课题的研究意义主要体现在以下几个方面：

（1）**理论意义**：本文系统性地研究了微服务架构在电商领域的应用，分析了微服务架构的设计原则、服务拆分策略、服务治理机制等核心问题，为相关领域的研究提供了参考案例。

（2）**实践意义**：本文设计并实现了一个功能完整的分布式电商系统，涵盖了电商系统的核心业务流程，对同类系统的开发具有实际的参考价值。

（3）**技术价值**：系统采用Spring Cloud Alibaba技术栈，整合了Nacos、RabbitMQ、Redis等主流中间件，展示了微服务架构的实际落地方案，对学习和实践微服务技术的开发者具有指导意义。

### 1.2 国内外研究现状

#### 1.2.1 微服务架构研究现状

微服务架构的概念最早由Martin Fowler于2014年提出，其核心思想是将单体应用拆分为一组小型服务，每个服务运行在独立的进程中，服务之间通过轻量级通信机制（通常是HTTP RESTful API）进行交互。

在国外，Netflix、Amazon、Google等互联网巨头较早开始实践微服务架构。Netflix开源了Eureka、Hystrix、Zuul等一系列微服务组件，形成了完整的微服务解决方案。Spring Cloud在Netflix开源组件的基础上，提供了一套完整的微服务开发框架。

在国内，阿里巴巴基于自身的业务实践，开发了Spring Cloud Alibaba技术栈，提供了Nacos（服务注册与配置中心）、Sentinel（流量控制）、Seata（分布式事务）等组件。Spring Cloud Alibaba已成为国内微服务开发的主流选择。

#### 1.2.2 电商系统研究现状

目前市面上的电商系统主要分为两类：

（1）**大型电商平台**：如淘宝、京东、拼多多等，这些平台采用高度定制化的技术架构，系统复杂度极高，一般企业难以复制。

（2）**开源电商系统**：如基于PHP的Magento、基于Java的Shopizer等，这些系统功能较为完整，但多采用传统的单体架构或较早期的技术栈。

近年来，基于微服务架构的开源电商系统逐渐增多，如Pig、RuoYi-Cloud等，但这些系统多为通用的后台管理系统，电商业务功能相对简单。因此，开发一个功能完整、技术先进的微服务电商系统仍具有实际意义。

### 1.3 研究内容与论文结构

本文的主要研究内容包括：

（1）分析电商系统的业务需求，确定系统的功能模块和非功能需求。

（2）设计基于微服务架构的系统整体架构，包括服务拆分策略、数据库设计、接口设计等。

（3）实现系统的核心功能模块，包括用户服务、商品服务、订单服务、购物车服务、支付服务等。

（4）集成Nacos、RabbitMQ、Redis等中间件，实现服务注册与发现、消息队列、缓存等功能。

（5）开发基于Vue 3的前端系统，实现响应式用户界面。

（6）对系统进行功能测试和性能测试，验证系统的正确性和性能。

论文共分为七章，各章内容安排如下：

第一章：绪论。介绍研究背景与意义、国内外研究现状、研究内容与论文结构。

第二章：相关技术介绍。介绍系统开发所使用的关键技术，包括Spring Boot、Spring Cloud Alibaba、Vue.js等。

第三章：系统需求分析。分析系统的功能需求和非功能需求，确定系统的开发目标。

第四章：系统设计。介绍系统的整体架构设计、数据库设计、接口设计等。

第五章：系统实现。详细介绍各功能模块的实现过程和关键代码。

第六章：系统测试。介绍测试环境、测试方法和测试结果。

第七章：总结与展望。总结本文的研究成果，分析存在的不足，展望未来的研究方向。

---

## 第二章 相关技术介绍

### 2.1 Spring Boot框架

#### 2.1.1 Spring Boot概述

Spring Boot是由Pivotal团队开发的用于简化Spring应用开发的框架。它采用"约定优于配置"的设计理念，能够快速创建独立的、生产级别的Spring应用程序。

Spring Boot的主要特点包括：

（1）**简化配置**：通过自动配置机制，减少了大量的XML配置和样板代码。

（2）**内嵌容器**：内置Tomcat、Jetty等Servlet容器，应用程序可以独立运行，无需部署到外部容器。

（3）**起步依赖**：提供一系列starter依赖，简化了依赖管理。

（4）**生产就绪**：提供健康检查、外部化配置、指标监控等功能。

#### 2.1.2 Spring Boot 3.x新特性

本系统采用Spring Boot 3.3.4版本，该版本的主要特性包括：

（1）**Java 17基线**：要求最低使用Java 17，支持最新的Java语言特性。

（2）**原生编译支持**：支持GraalVM原生编译，显著提升应用启动速度和内存占用。

（3）**可观测性增强**：集成了Micrometer，提供了更完善的监控指标。

（4）**安全性提升**：升级了安全框架，修复了多个安全漏洞。

### 2.2 Spring Cloud Alibaba

#### 2.2.1 Spring Cloud Alibaba概述

Spring Cloud Alibaba是阿里巴巴开源的微服务开发框架，提供了微服务开发所需的核心组件。它遵循Spring Cloud的标准规范，与Spring Cloud生态系统无缝集成。

Spring Cloud Alibaba的核心组件包括：

（1）**Nacos**：动态服务发现、配置管理和服务管理平台。

（2）**Sentinel**：流量控制、熔断降级、系统负载保护的高可用防护组件。

（3）**Seata**：高性能微服务分布式事务解决方案。

（4）**RocketMQ**：分布式消息中间件。

#### 2.2.2 Nacos服务注册与配置中心

Nacos（Dynamic Naming and Configuration Service）是阿里巴巴开源的服务发现和配置管理平台。它提供了以下核心功能：

（1）**服务注册与发现**：服务提供者将自身注册到Nacos Server，服务消费者从Nacos Server获取服务提供者列表，实现服务的自动发现。

（2）**动态配置管理**：支持配置的动态更新，无需重启服务即可生效。

（3）**服务健康检查**：支持主动健康检查和被动健康检查，自动剔除不健康的服务实例。

本系统使用Nacos 2.4.3版本，采用单机模式部署。服务启动时，自动向Nacos注册服务实例，并从Nacos获取配置信息。

**（一）Nacos 集群一致性：Raft 算法分析**

Nacos 2.x 的配置中心集群采用 Raft 共识算法保证数据一致性。Raft 将分布式一致性问题分解为三个相对独立的子问题：领导者选举（Leader Election）、日志复制（Log Replication）和安全性（Safety）。

设集群由 $n$ 个节点组成，Raft 要求多数派（Quorum）即 $\lceil n/2 \rceil + 1$ 个节点同意才能提交日志条目。集群最多可容忍 $f$ 个故障节点，其中：

$$f < \frac{n}{2}$$

**领导者选举机制**：每个节点初始状态为 Follower，若在随机超时时间 $T_{election}$ 内未收到 Leader 心跳，则转变为 Candidate 发起选举：

$$T_{election} \in [T_{min},\ T_{max}] = [150ms,\ 300ms]$$

候选者获得多数票 $\lceil n/2 \rceil + 1$ 后成为 Leader，每隔心跳间隔 $T_{heartbeat} = 50ms$ 向所有 Follower 发送心跳以维持领导权。

**日志一致性证明**：设 Leader 任期为 $t$，日志索引为 $i$，若两条日志条目 $\langle t_1, i_1 \rangle = \langle t_2, i_2 \rangle$，则这两条日志完全相同（Raft 日志匹配性质）：

$$\forall\ i: entry_1[i] = entry_2[i] \Rightarrow entry_1[0..i] = entry_2[0..i]$$

**（二）Nacos 服务注册一致性哈希分片**

Nacos 集群内部通过一致性哈希环（Consistent Hash Ring）分配数据。节点 $v_i$ 的虚拟节点哈希值为：

$$h(v_i^k) = \text{MD5}(ip_i + ":" + port_i + "\#" + k) \bmod 2^{32},\quad k = 1, 2, \ldots, 150$$

每个服务实例通过其 IP + Port 计算哈希值，映射到哈希环上最近的虚拟节点负责的区间，从而确定数据归属节点。当节点加入或退出时，只有相邻区间的数据迁移，减小了数据迁移量。

图 2-1 展示了 Nacos 服务注册与发现的完整交互时序：

```
服务提供者          Nacos Server         服务消费者
   │                    │                    │
   │── register ────────▶│                    │
   │   {ip,port,         │                    │
   │    serviceName}     │                    │
   │◀── 200 OK ──────────│                    │
   │                    │                    │
   │── heartbeat ───────▶│  (每5秒发送心跳)    │
   │                    │                    │
   │                    │◀── subscribe ───────│
   │                    │   {serviceName}     │
   │                    │── push instances ──▶│
   │                    │   [{ip,port}×N]     │
   │                    │                    │
```

图2-1 Nacos 服务注册与发现时序图

**（三）Nacos 健康检查机制**

Nacos 对临时实例（ephemeral=true）采用客户端心跳检测模式，心跳上报间隔 $T_h = 5s$，健康检查超时阈值：

- 标记不健康：$T_{unhealthy} = 15s$（错过 3 次心跳）
- 剔除实例：$T_{delete} = 30s$（错过 6 次心跳）

设服务实例集合为 $S = \{s_1, s_2, \ldots, s_n\}$，健康实例集合为：

$$S_{healthy} = \{s_i \in S\ |\ \Delta t_i < T_{unhealthy}\}$$

其中 $\Delta t_i$ 为实例 $s_i$ 距上次心跳的时间间隔。消费者只从 $S_{healthy}$ 中选择实例进行调用，保证了请求不会路由到故障节点。

#### 2.2.3 OpenFeign 声明式 HTTP 客户端

Spring Cloud OpenFeign 是一种声明式的 HTTP 客户端，通过 Java 接口和注解描述 HTTP 请求，屏蔽了底层 HTTP 通信细节，并与 Ribbon/LoadBalancer 深度集成实现客户端负载均衡。

**（一）OpenFeign 工作原理**

OpenFeign 的核心原理是动态代理。启动时，`@EnableFeignClients` 触发扫描，为每个标注了 `@FeignClient` 的接口生成 `ReflectiveFeign` 代理对象。调用时，代理对象：

1. 将方法调用转换为 `RequestTemplate`（包含 URL、Header、Body）；
2. 通过 `LoadBalancerClient` 从 Nacos 服务列表中选取一个实例；
3. 将 `RequestTemplate` 渲染为完整 HTTP 请求并执行；
4. 将响应反序列化为方法返回类型。

图 2-2 为 OpenFeign 调用链路示意图：

```
调用方法
   │
   ▼
ReflectiveFeign.invoke()
   │
   ▼
SynchronousMethodHandler.invoke()
   │  ① 构建 RequestTemplate
   ▼
LoadBalancerFeignClient.execute()
   │  ② 从 Nacos 获取实例列表
   │  ③ 负载均衡选取实例
   ▼
AbstractLoadBalancerAwareClient.executeWithLoadBalancer()
   │  ④ 发起 HTTP 请求
   ▼
响应解码 → 返回结果
```

图2-2 OpenFeign 调用链路图

**（二）客户端负载均衡算法**

Spring Cloud LoadBalancer 默认采用**加权轮询（Weighted Round Robin）**算法。设服务实例集合为 $\mathbf{S} = \{s_1, s_2, \ldots, s_n\}$，各实例权重为 $\mathbf{w} = \{w_1, w_2, \ldots, w_n\}$，总权重：

$$W = \sum_{i=1}^{n} w_i$$

轮询周期为 $W$ 次请求，第 $k$ 次请求选取实例 $s_j$，使得：

$$j = \arg\min_{i} \left\{ c_i - w_i \right\}, \quad \text{其中}\ c_i \leftarrow c_i + w_i - W \cdot \mathbb{1}[i = j]$$

此算法确保了在 $W$ 次请求内，实例 $s_i$ 恰好被选取 $w_i$ 次，实现了按权分配。

**重试机制**：当请求失败时，OpenFeign 支持重试，设最大重试次数为 $R_{max}$，重试间隔指数退避：

$$T_{retry}^{(r)} = T_0 \cdot 2^{r-1},\quad r = 1, 2, \ldots, R_{max}$$

本系统配置 `maxAttempts=3`，`period=100ms`，即最多重试 2 次，初始间隔 100ms。

**（三）OpenFeign 超时配置分析**

OpenFeign 的超时由两层控制：

| 参数 | 作用 | 本系统配置 |
|------|------|-----------|
| connectTimeout | TCP 连接超时 | 5000ms |
| readTimeout | 等待响应超时 | 10000ms |

当读超时发生时，结合 Sentinel 的熔断规则，触发降级处理，保护调用链路不被阻塞。

#### 2.2.4 Sentinel 流量控制与熔断降级

Sentinel 是阿里巴巴开源的高可用防护组件，提供流量控制、熔断降级、系统自适应保护三大能力。

**（一）令牌桶限流算法**

Sentinel 流量控制的核心是令牌桶（Token Bucket）算法。设令牌桶容量为 $C$（最大突发量），令牌生成速率为 $r$（QPS 阈值），当前桶中令牌数为 $N(t)$：

$$N(t) = \min\left(C,\ N(t - \Delta t) + r \cdot \Delta t\right)$$

每个请求到达时消耗一个令牌：
- 若 $N(t) \geq 1$：允许请求通过，$N(t) \leftarrow N(t) - 1$
- 若 $N(t) < 1$：拒绝请求或排队等待

当系统设置 QPS 阈值 $r = 100$ 时，令牌每 $10ms$ 生成一个。突发流量到来时，桶中积累的令牌可吸收瞬时峰值，最大突发量由 $C$ 控制（本系统 $C = 200$）。

**（二）滑动窗口统计算法**

Sentinel 使用滑动时间窗口统计 QPS。将时间轴按大小为 $W$（统计周期，默认 1000ms）的窗口分割，每个窗口内部再分为 $K$（默认 2）个 bucket，每个 bucket 时长 $t_s = W/K = 500ms$。

设当前时刻为 $t$，有效窗口覆盖范围 $[t - W, t]$，当前 QPS 估计值：

$$QPS(t) = \frac{\sum_{k=0}^{K-1} count\left(bucket\left(\lfloor t/t_s \rfloor - k\right)\right)}{W / 1000}$$

当 $QPS(t) > threshold$ 时触发限流。滑动窗口相比固定窗口消除了边界突刺问题，统计更加平滑准确。

图 2-3 为滑动窗口 QPS 统计示意图：

```
时间轴（ms）:  0    500   1000  1500  2000
              │     │     │     │     │
bucket编号:  [0]   [1]   [2]   [3]   [4]
              ↑过期  ↑过期  ↑有效  ↑有效  ↑当前

当前时刻 t=1800ms，W=1000ms，K=2
有效bucket: [2](1000-1500ms) + [3](1500-1800ms)
QPS = (count[2] + count[3]) / 1.0
```

图2-3 Sentinel 滑动窗口 QPS 统计

**（三）熔断器状态机模型**

Sentinel 的熔断降级基于三状态有限状态机（FSM）：

$$States = \{CLOSED,\ OPEN,\ HALF\_OPEN\}$$

状态转移规则如下：

| 当前状态 | 转移条件 | 目标状态 |
|----------|----------|----------|
| CLOSED | 错误率 $\geq$ 阈值 $\varepsilon$ 且请求数 $\geq$ minRequest | OPEN |
| OPEN | 熔断时长 $T_{break}$ 到期 | HALF_OPEN |
| HALF_OPEN | 探测请求成功 | CLOSED |
| HALF_OPEN | 探测请求失败 | OPEN |

设时间窗口内请求总数为 $N$，失败请求数为 $F$，错误率：

$$\varepsilon_{actual} = \frac{F}{N}$$

当 $\varepsilon_{actual} \geq \varepsilon_{threshold}$ 且 $N \geq N_{min}$（最小请求数，防止低流量误触发）时，熔断器从 CLOSED 转为 OPEN，在 $T_{break}$（默认 5s）内所有请求直接返回降级响应，之后进入 HALF_OPEN 探测状态。

图2-4 熔断器状态机转移图：

```
        错误率超阈值
CLOSED ──────────────▶ OPEN
  ▲                      │
  │  探测成功            │ T_break 超时
  │                      ▼
  └──────────── HALF_OPEN
                探测失败 ──▶ OPEN (重置计时)
```

图2-4 Sentinel 熔断器状态机

**（四）系统自适应保护**

Sentinel 提供系统级别的自适应保护，基于 Linux 系统负载 $load_1$（1分钟平均负载）和响应时间 $RT$ 动态计算并发线程数上限：

$$maxThread = \frac{minRT \cdot maxQPS}{1000}$$

当实际并发线程数超过 $maxThread$ 时，触发系统保护，拒绝新请求，防止系统过载崩溃。

#### 2.2.5 Spring Cloud Gateway API 网关

Spring Cloud Gateway 是基于 Spring WebFlux（响应式编程）的 API 网关，采用非阻塞 I/O 模型，具有高吞吐量的特点。

**（一）响应式 I/O 性能模型**

传统阻塞型网关（如 Zuul 1.x）采用"每请求一线程"模型，最大并发数受线程池大小限制：

$$C_{blocking} \leq T_{pool} \approx 200 \sim 1000$$

Spring Cloud Gateway 基于 Reactor Netty，采用事件循环（Event Loop）模型，少量线程（通常等于 CPU 核心数 $n_{cpu}$）即可处理大量并发请求：

$$C_{reactive} \gg C_{blocking}, \quad threads = n_{cpu}\ (通常\ 8 \sim 16)$$

吞吐量理论上界（Amdahl's Law 推论）：

$$Throughput = \frac{n_{cpu}}{RT_{avg}} \times 1000 \text{ (req/s)}$$

**（二）路由匹配算法**

Gateway 将路由规则组织为有序列表 $R = [r_1, r_2, \ldots, r_m]$，每个路由 $r_i$ 包含谓词集合 $P_i$ 和过滤器链 $F_i$。请求匹配过程：

$$match(req, r_i) = \bigwedge_{p \in P_i} p(req)$$

按路由 `order` 值升序遍历，返回第一个满足 $match(req, r_i) = true$ 的路由 $r_i$：

$$route(req) = r_j,\quad j = \min\{i\ |\ match(req, r_i) = true\}$$

**（三）过滤器链执行模型**

Gateway 过滤器分为全局过滤器（GlobalFilter）和路由过滤器（GatewayFilter），执行顺序由 `Ordered` 接口的 `getOrder()` 值决定：

设过滤器序列为 $\mathbf{F} = [f_1, f_2, \ldots, f_k]$，按 `order` 值升序排列，执行过程形如递归调用链：

$$chain = f_1 \circ f_2 \circ \cdots \circ f_k \circ proxied\_service$$

请求阶段按 $f_1 \to f_2 \to \cdots \to f_k$ 执行，响应阶段按 $f_k \to \cdots \to f_2 \to f_1$ 执行（类似 AOP 环绕通知）。

图 2-5 为 Gateway 请求处理流程：

```
客户端请求
    │
    ▼
HttpWebHandlerAdapter
    │
    ▼
DispatcherHandler
    │
    ▼
RoutePredicateHandlerMapping   ← 匹配路由规则
    │  (Path、Method、Header等谓词)
    ▼
FilteringWebHandler
    │
    ├── GlobalFilter: AuthFilter (order=-100)  ← Token验证
    ├── GlobalFilter: LogFilter  (order=0)     ← 请求日志
    ├── GatewayFilter: StripPrefix             ← 去除/api前缀
    ▼
Proxied Microservice (lb://service-xxx)
    │  通过 LoadBalancerClient 选取实例
    ▼
响应返回客户端
```

图2-5 Gateway 请求处理流程图

**（四）与 Nacos 集成的服务发现路由**

Gateway 通过 `lb://serviceName` 协议与 Nacos 集成，实现动态路由。URI 前缀 `lb://` 表示通过 LoadBalancer 进行负载均衡，`serviceName` 即 Nacos 中注册的服务名。

当 Nacos 中服务实例列表发生变化（实例上下线）时，Gateway 通过 Spring Cloud ServiceInstanceListSupplier 的缓存刷新机制（默认 30s 刷新一次）同步最新实例列表，无需重启网关即可感知服务变化，实现动态路由。

### 2.3 MyBatis Plus

MyBatis Plus是MyBatis的增强工具，在MyBatis的基础上提供了通用CRUD操作、代码生成器、分页插件等功能，极大地简化了数据访问层的开发。

MyBatis Plus的主要特性包括：

（1）**通用CRUD**：内置通用Mapper和Service，通过少量配置即可实现单表大部分CRUD操作。

（2）**条件构造器**：提供了强大的条件构造器，支持复杂的查询条件组装。

（3）**分页插件**：内置物理分页插件，支持多种数据库。

（4）**代码生成器**：可根据数据库表结构自动生成Entity、Mapper、Service等代码。

本系统使用MyBatis Plus作为持久层框架，通过Lambda条件构造器实现类型安全的查询操作。

### 2.4 Vue.js框架

#### 2.4.1 Vue.js概述

Vue.js是一个渐进式JavaScript框架，用于构建用户界面。它的核心库只关注视图层，易于上手，同时也能够驱动复杂的单页应用程序。

Vue.js的主要特点包括：

（1）**响应式数据绑定**：采用数据驱动视图的方式，数据变化时视图自动更新。

（2）**组件化开发**：将界面拆分为独立的组件，提高代码的复用性和可维护性。

（3）**虚拟DOM**：使用虚拟DOM提高渲染性能。

（4）**生态系统完善**：配套的Vue Router、Vuex/Pinia等库提供了完整的解决方案。

#### 2.4.2 Vue 3新特性

本系统采用Vue 3.x版本，相比Vue 2.x有以下主要改进：

（1）**Composition API**：提供了更灵活的代码组织方式，便于逻辑复用。

（2）**性能提升**：通过静态节点提升、事件缓存等优化，渲染性能显著提升。

（3）**更好的TypeScript支持**：源码使用TypeScript重写，提供更好的类型支持。

（4）**更小的打包体积**：支持Tree-shaking，未使用的代码不会被打包。

#### 2.4.3 Element Plus组件库

Element Plus是基于Vue 3的企业级UI组件库，提供了丰富的PC端组件，包括表单、表格、弹窗、导航等常用组件。本系统使用Element Plus构建管理后台和用户界面，主要使用了以下组件：

（1）**表单组件**：el-form、el-input、el-select、el-radio等。

（2）**数据展示组件**：el-table、el-pagination、el-card等。

（3）**导航组件**：el-menu、el-tabs、el-breadcrumb等。

（4）**反馈组件**：el-message、el-dialog、el-loading等。

### 2.5 Redis缓存

Redis是一个开源的高性能键值对存储数据库，支持多种数据结构，包括字符串、哈希、列表、集合、有序集合等。Redis的主要特点包括：

（1）**高性能**：基于内存操作，读写性能极高，单机QPS可达10万以上。

（2）**数据持久化**：支持RDB和AOF两种持久化方式，保证数据安全。

（3）**丰富的数据类型**：支持字符串、哈希、列表、集合、有序集合等数据类型。

（4）**原子操作**：所有操作都是原子性的，支持事务。

本系统使用Redis实现订单超时自动取消功能，将订单创建时间存储在Redis中，通过定时任务检查超时订单。

### 2.6 RabbitMQ消息队列

RabbitMQ是一个开源的消息代理软件，实现了高级消息队列协议（AMQP）。它的主要特点包括：

（1）**可靠性**：支持消息持久化、确认机制、事务等，保证消息的可靠传递。

（2）**灵活的路由**：通过交换机和队列的组合，实现灵活的消息路由。

（3）**多语言客户端**：支持Java、Python、JavaScript等多种语言的客户端。

（4）**管理界面**：提供Web管理界面，方便监控和管理。

本系统使用RabbitMQ实现订单和支付服务之间的异步通信，当订单支付成功后，通过消息队列通知支付服务更新订单状态。

### 2.7 MySQL数据库

MySQL是最流行的开源关系型数据库管理系统，具有体积小、速度快、成本低等优势。MySQL 8.0版本引入了许多新特性：

（1）**窗口函数**：支持ROW_NUMBER、RANK等窗口函数。

（2）**CTE（公共表表达式）**：支持递归CTE，简化复杂查询。

（3）**JSON增强**：提供更完善的JSON支持。

（4）**性能优化**：改进了InnoDB存储引擎的性能。

本系统使用MySQL 8.0作为主数据库，存储用户、商品、订单等核心业务数据。

---

## 第三章 系统需求分析

### 3.1 系统概述

飞鱼商城是一个面向消费者的综合性B2C电商平台，销售商品涵盖生鲜食品、冷冻食品、休闲零食、水产干货、日用品等多个品类。系统采用B2C（Business to Consumer）商业模式，商家在后台管理商品和订单，消费者在前台浏览商品、下单购买。系统以"微服务架构 + 前后端分离"为技术路线，后端基于Spring Cloud Alibaba构建分布式服务集群，前端采用Vue 3实现响应式用户界面，旨在打造一个功能完整、架构先进、易于扩展的电商系统。

系统的核心业务流程覆盖了消费者从浏览到售后的完整购物链路：用户注册登录后，可浏览商品列表、按分类或关键词搜索商品、查看商品详情；将心仪商品加入购物车后，可选择收货地址并提交订单；订单支付后由商家发货，用户确认收货完成交易。后台管理系统提供商品管理、订单管理、用户管理、库存预警、数据概览等功能，支撑平台运营。

系统整体架构分为以下层次：

（1）**接入层**：Nginx作为反向代理，统一接收前端请求并转发至API网关，同时负责静态资源托管和负载均衡。

（2）**网关层**：Spring Cloud Gateway作为统一入口，实现路由分发、JWT鉴权、Sentinel限流、请求日志记录等职责，所有微服务无需直接暴露端口。

（3）**服务层**：核心微服务包括用户服务（注册登录、信息管理、地址管理）、商品服务（商品CRUD、分类管理、标签管理、库存管理）、订单服务（订单创建、状态流转、超时取消）、购物车服务（商品暂存、数量修改、价格同步）、支付服务（支付处理、回调通知、销量更新）、内容服务（视频、图库、公告管理）、AI服务（智能客服对话），各服务独立部署、独立扩展。

（4）**治理层**：Nacos提供服务注册与配置中心，Sentinel提供流量控制与熔断降级，Spring Boot Admin提供服务监控，共同保障微服务集群的高可用运行。

（5）**数据层**：MySQL 8.0存储核心业务数据（采用每个服务独立数据库的多库分治策略），Redis实现缓存加速和订单超时自动取消，RabbitMQ实现异步消息通信。

系统技术栈总结如下表所示：

| 层次 | 技术选型 | 版本 |
| --- | --- | --- |
| 后端框架 | Spring Boot | 3.3.4 |
| 微服务框架 | Spring Cloud Alibaba | 2023.0.3.2 |
| 服务注册与配置 | Nacos | 2.4.3 |
| API网关 | Spring Cloud Gateway | - |
| 流量控制 | Sentinel | 1.8.7 |
| 声明式调用 | OpenFeign | 4.1.0 |
| 关系数据库 | MySQL | 8.0 |
| 缓存 | Redis | 6.0 |
| 消息队列 | RabbitMQ | 3.12 |
| 系统监控 | Spring Boot Admin | 3.3.4 |
| 前端框架 | Vue 3 + Element Plus | 3.4 / 2.5 |
| 构建工具 | Maven / Vite | 3.9 / 5.0 |
| 容器化 | Docker + Docker Compose | - |

### 3.2 功能需求分析

#### 3.2.1 用户管理

系统为用户提供注册登录功能，并且支持用户修改个人信息、收货地址、登录密码等操作。同时为用户和游客开放不同的使用权限，游客仅支持浏览操作，登录用户可进行加购、下单、支付、评价等完整购物操作。具体需求如下：

（1）**用户注册**：用户通过填写用户名、密码完成账号注册，系统校验用户名唯一性，注册成功后自动登录。

（2）**用户登录**：用户输入用户名和密码登录系统，登录成功后服务端签发 JWT Token，前端将其存储并在后续请求中携带，网关层通过 AuthFilter 对非白名单接口统一校验 Token 有效性。

（3）**个人信息管理**：登录用户可查看和修改个人资料，包括昵称、头像、手机号、邮箱、生日等信息。

（4）**收货地址管理**：用户可添加、编辑、删除收货地址，支持设置默认地址，下单时自动填充默认收货信息。

（5）**密码修改**：用户可在个人中心修改登录密码，保障账号安全。

#### 3.2.2 商品管理

系统提供商品浏览功能，为用户展示商品详情页，包含商品基本信息（名称、价格、销量、库存）、用户评价列表以及推荐商品模块。用户可以根据自己的购物体验对商品发表评论和打分，评论后对于自己不满意的评价也可以进行删除或修改。用户也可以在首页查看各类商品的热销榜单和新品榜单，根据榜单上的商品进行选购能够更快找到心仪的商品。具体需求如下：

（1）**商品列表**：系统以分页形式展示商品列表，支持按分类筛选、按关键词搜索，并提供默认排序、价格升序/降序、销量排序等多种排列方式。

（2）**商品详情**：点击商品进入详情页，展示商品名称、价格、原价、规格选项、产地、重量、保质期、储存方式等完整信息，并以 Markdown 格式渲染商品图文详情。页面同时展示用户评价列表和同分类推荐商品。

（3）**商品分类**：系统按品类对商品进行分组（如生鲜食品、休闲零食、冷冻速食、水产干货等），用户可在商品列表页通过左侧分类导航快速切换品类。

（4）**商品标签**：商品支持多标签标记（如热卖爆款、进口精品、新鲜直达、清热解毒等），用户可在列表页通过标签筛选快速定位商品。

（5）**热门推荐**：首页展示热门商品推荐列表，按销量自动排序，帮助用户快速发现畅销商品。

（6）**轮播图**：首页顶部展示轮播图，管理员可设置指定商品为轮播推荐，点击轮播图直接跳转商品详情。

（7）**商品评价**：登录用户可对已购买商品发表文字评论和评分，可修改或删除自己的评价。商品详情页展示评价列表，供其他用户参考。

#### 3.2.3 购物车管理

系统提供购物车功能，用户可将心仪商品暂存至购物车，在购物车页面统一管理待购商品并批量结算。具体需求如下：

（1）**加入购物车**：用户在商品详情页选择规格和数量后，点击加入购物车按钮将商品加入购物车。若同一商品已存在于购物车，则自动累加数量。

（2）**数量修改**：用户可在购物车页面通过增减按钮或直接输入修改商品数量，系统实时更新小计金额和合计金额。

（3）**删除商品**：用户可删除购物车中的单个商品，也可批量清除已选商品。

（4）**价格同步**：购物车中的商品价格与商品服务保持实时同步，当商品价格发生变动时，购物车自动展示最新价格，避免价格差异导致的交易纠纷。

（5）**结算跳转**：用户在购物车勾选待购商品后，点击结算按钮进入订单确认页面，选择收货地址和支付方式后提交订单。

（6）**猜你喜欢**：购物车页面下方展示个性化推荐商品，基于用户已加购商品的分类信息推荐相关商品，引导用户继续选购。

#### 3.2.4 订单管理

系统提供订单功能，在购物车页面用户可以勾选商品并创建订单，支持填写收货地址、选择支付方式。订单生成后，用户可在“我的订单”页面查看订单状态。用户可以对待支付订单进行支付操作或取消订单，对已收货订单进行确认收货和评价。同时系统支持订单超时自动取消机制，对于超过2分钟未支付的订单自动释放库存并关闭订单，确保库存资源的有效利用。用户还可以查看历史订单记录，方便进行复购操作。具体需求如下：

（1）**创建订单**：用户在购物车勾选商品后提交订单，系统校验库存充足后生成订单，同时锁定对应商品库存。

（2）**订单状态流转**：订单状态按“待付款→待发货→待收货→已完成”依次流转，每种状态对应不同的用户可操作项。

（3）**订单支付**：用户对待支付订单选择支付方式并完成支付，支付成功后订单状态更新为待发货，同时调用商品服务更新销量。

（4）**订单超时自动取消**：系统基于 Redis 延迟队列实现超时机制，订单创建2分钟后若仍未支付则自动取消，释放已锁定的库存资源。

（5）**订单发货**：管理员在后台对待发货订单执行发货操作，订单状态更新为待收货。

（6）**确认收货**：用户收到商品后点击确认收货，订单状态更新为已完成，用户可对已完成订单进行评价。

（7）**订单查询**：用户可按状态（全部、待付款、待发货、待收货、已完成）筛选查看订单列表，查看订单详情和商品信息。

#### 3.2.5 支付管理

系统提供在线支付功能，支持模拟支付和真实第三方支付两种模式。用户在订单确认页选择支付方式后，系统处理支付流程并通知订单服务更新状态。具体需求如下：

（1）**支付方式选择**：用户在支付页面可选择支付宝或微信支付两种支付方式。

（2）**支付处理**：用户确认支付后，支付服务处理支付逻辑，通过 RabbitMQ 异步通知订单服务更新支付状态，同时通过 OpenFeign 同步调用商品服务更新销量，保证数据一致性。

（3）**支付回调**：支付完成后系统接收回调通知，校验支付结果后更新订单状态为待发货。

（4）**支付状态查询**：用户和后台管理员可查询订单的支付状态和支付方式信息。

#### 3.2.6 后台管理

系统为管理员提供功能完备的后台管理平台，涵盖商品运营、订单处理、用户管理、内容维护等核心运营能力。后台管理采用独立的管理员登录入口，与前台用户体系隔离，保障管理操作的安全性。具体需求如下：

（1）**管理员登录**：管理员使用独立账号密码登录后台管理系统，与普通用户登录入口分离。

（2）**数据概览**：后台首页展示核心运营指标，包括总用户数、今日订单数、今日成交额、商品总数，以及近7天订单趋势图和订单状态分布图，帮助管理员快速掌握平台运营状况。

（3）**商品管理**：管理员可添加、编辑商品信息（名称、价格、库存、分类、规格、产地、保质期等），上传商品主图和详情图，控制商品上下架状态，设置轮播图推荐。

（4）**订单管理**：管理员可查看全平台订单列表，按状态筛选订单，对待发货订单执行发货操作。

（5）**用户管理**：管理员可查看用户列表和用户画像信息，了解用户消费偏好。

（6）**分类与标签管理**：管理员可对商品分类和标签进行增删改查，维护商品的组织结构。

（7）**库存管理**：管理员可查看库存预警列表（库存低于阈值的商品标红提示），快速调整商品库存数量，避免缺货。

（8）**评论管理**：管理员可查看商品评论列表，对违规评论进行删除处理。

（9）**内容管理**：管理员可管理公告、视频、图库等内容，维护平台信息展示。

（10）**系统监控**：集成 Spring Boot Admin 监控面板，管理员可实时查看各微服务的运行状态、JVM 内存、线程数、GC 统计等指标，及时发现和处理系统异常。

#### 3.2.7 AI客服

系统集成 AI 智能客服功能，为用户提供在线咨询服务。AI客服基于大语言模型实现，能够理解用户意图并提供商品推荐、购物指导等智能回复，提升用户购物体验。具体需求如下：

（1）**浮动入口**：页面右下角固定显示AI助手浮动图标，用户点击即可唤起对话窗口，不影响正常浏览操作。

（2）**对话交互**：用户在对话窗口中输入问题，AI客服通过调用大语言模型接口生成智能回复，支持流式输出（逐字显示），提升交互体验的流畅感。

（3）**免登录使用**：AI客服功能对所有用户开放，无需登录即可使用，降低使用门槛，为游客提供商品咨询和购物引导。

（4）**商品知识问答**：AI客服具备商品知识库能力，能够回答商品信息、分类推荐、优惠活动等与平台业务相关的问题，充当智能导购角色。

### 3.3 非功能需求分析

【此处插入用例图】

#### 3.3.1 性能需求

（1）**响应时间**：页面加载时间不超过3秒，接口响应时间不超过1秒。

（2）**并发能力**：系统应支持1000并发用户的访问。

（3）**数据库性能**：数据库查询应使用索引优化，避免全表扫描。

#### 3.3.2 可用性需求

（1）**系统可用性**：系统可用性应达到99%以上。

（2）**故障恢复**：单个服务故障不影响其他服务的正常运行。

（3）**数据备份**：数据库应定期备份，保证数据安全。

#### 3.3.3 安全性需求

（1）**身份认证**：用户和管理员需要登录才能访问相应功能。

（2）**权限控制**：不同角色的用户只能访问其权限范围内的功能。

（3）**数据加密**：用户密码应使用加密存储，敏感数据传输应使用HTTPS。

（4）**防攻击**：系统应能防止SQL注入、XSS攻击等常见安全威胁。

#### 3.3.4 可扩展性需求

（1）**水平扩展**：系统应支持服务的水平扩展，通过增加实例提高处理能力。

（2）**模块化设计**：系统应采用模块化设计，便于添加新功能。

（3）**接口标准化**：系统接口应遵循RESTful规范，便于与其他系统集成。

---

## 第四章 系统设计

### 4.1 系统架构设计

#### 4.1.1 整体架构

本系统采用前后端分离的微服务架构，整体架构分为三层：

（1）**前端层**：包括用户端和管理端，用户端面向消费者，管理端面向管理员。前端通过HTTP请求调用后端API。

（2）**网关层**：使用Spring Cloud Gateway作为API网关，负责请求路由、负载均衡、身份认证等功能。

（3）**服务层**：包括多个独立的微服务，每个服务负责特定的业务功能，服务之间通过Feign进行调用。

（4）**数据层**：包括MySQL数据库、Redis缓存等。

系统架构图如下：

【此处插入系统架构图】

#### 4.1.2 服务架构图

图4-1为系统的微服务整体架构，体现了分布式系统的核心组件协作关系：

```
                    ┌─────────────────────────────────┐
                    │         客户端（浏览器）           │
                    │   用户端：localhost:5173          │
                    │   管理端：localhost:5173/admin    │
                    └──────────────┬──────────────────┘
                                   │ HTTP/HTTPS
                                   ▼
                    ┌─────────────────────────────────┐
                    │    Spring Cloud Gateway (5000)   │
                    │  ┌──────────────────────────┐   │
                    │  │ AuthFilter (Token验证)    │   │
                    │  │ LogFilter  (请求日志)     │   │
                    │  │ RateLimitFilter (限流)    │   │
                    │  └──────────────────────────┘   │
                    └──────────────┬──────────────────┘
                                   │ lb:// 负载均衡路由
           ┌───────────────────────┼───────────────────────┐
           │                       │                       │
           ▼                       ▼                       ▼
   ┌──────────────┐     ┌──────────────────┐     ┌──────────────────┐
   │service-user  │     │ service-product  │     │  service-order   │
   │   (7000)     │     │    (9000)        │     │    (8000)        │
   └──────┬───────┘     └────────┬─────────┘     └────────┬─────────┘
          │  OpenFeign            │  OpenFeign              │  OpenFeign
          └──────────────────────┴─────────────────────────┘
                                   │
           ┌───────────────────────┼───────────────────────┐
           │                       │                       │
           ▼                       ▼                       ▼
   ┌──────────────┐     ┌──────────────────┐     ┌──────────────────┐
   │ service-cart │     │  service-pay     │     │   service-ai     │
   │   (7500)     │     │   (8500)         │     │   (9100)         │
   └──────────────┘     └──────────────────┘     └──────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
         ┌─────────────────┐           ┌─────────────────┐
         │   Nacos (8848)  │           │  Monitor (8080) │
         │ 服务注册/配置中心 │           │ Spring Boot Admin│
         └────────┬────────┘           └─────────────────┘
                  │
     ┌────────────┼─────────────────┐
     ▼            ▼                 ▼
┌────────┐  ┌──────────┐  ┌──────────────┐
│ MySQL  │  │  Redis   │  │  RabbitMQ    │
│ (3306) │  │  (6379)  │  │   (5672)     │
└────────┘  └──────────┘  └──────────────┘
```

图4-1 系统微服务架构图

#### 4.1.3 服务拆分策略

根据业务领域和功能边界，系统拆分为以下微服务：

| 服务名 | 端口 | 职责 |
|--------|------|------|
| gateway | 5000 | API网关，请求路由、负载均衡 |
| service-user | 7000 | 用户注册、登录、信息管理、收货地址 |
| service-product | 9000 | 商品管理、分类管理、标签管理、库存管理 |
| service-order | 8000 | 订单创建、状态管理 |
| service-cart | 7500 | 购物车管理 |
| service-pay | 8500 | 支付处理 |
| service-video | 9500 | 视频内容管理 |
| service-ai | 9600 | AI智能客服服务 |
| monitor | 8600 | 系统监控（Spring Boot Admin） |

服务拆分遵循以下原则：

（1）**单一职责**：每个服务只负责一个业务领域。

（2）**高内聚低耦合**：服务内部功能高度相关，服务之间依赖尽可能少。

（3）**独立部署**：每个服务可以独立开发、测试、部署。

### 4.2 数据库设计

#### 4.2.1 概念模型设计

根据系统需求，设计以下主要实体：

【此处插入E-R图】

（1）**用户(User)**：存储用户基本信息，包括用户名、密码、手机号、邮箱等。

（2）**商品(Product)**：存储商品信息，包括名称、价格、库存、图片等。

（3）**订单(Order)**：存储订单信息，包括订单号、用户ID、金额、状态等。

（4）**订单项(OrderItem)**：存储订单中的商品明细。

（5）**购物车(Cart)**：存储用户购物车信息。

#### 4.2.2 逻辑模型设计

**用户表(user)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码（加密） |
| nickname | VARCHAR(50) | | 昵称 |
| phone | VARCHAR(20) | UNIQUE | 手机号 |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| avatar | VARCHAR(255) | | 头像URL |
| birthday | DATE | | 生日 |
| status | TINYINT | DEFAULT 1 | 状态：0禁用，1正常 |
| create_time | DATETIME | | 创建时间 |
| update_time | DATETIME | | 更新时间 |

**商品表(product)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| name | VARCHAR(100) | NOT NULL | 商品名称 |
| description | TEXT | | 商品描述 |
| category_id | BIGINT | | 分类ID |
| price | DECIMAL(10,2) | NOT NULL | 价格 |
| original_price | DECIMAL(10,2) | | 原价 |
| stock | INT | DEFAULT 0 | 库存 |
| sales | INT | DEFAULT 0 | 销量 |
| main_image | VARCHAR(255) | | 主图URL |
| specs | JSON | | 规格信息 |
| origin | VARCHAR(100) | | 产地 |
| weight | VARCHAR(50) | | 重量 |
| shelf_life | VARCHAR(50) | | 保质期 |
| storage | VARCHAR(100) | | 储存方式 |
| status | TINYINT | DEFAULT 1 | 状态：0下架，1上架 |
| create_time | DATETIME | | 创建时间 |
| update_time | DATETIME | | 更新时间 |

**订单表(order)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| order_no | VARCHAR(50) | UNIQUE | 订单号 |
| user_id | BIGINT | NOT NULL | 用户ID |
| receiver_name | VARCHAR(50) | | 收货人姓名 |
| receiver_phone | VARCHAR(20) | | 收货人电话 |
| receiver_address | VARCHAR(255) | | 收货地址 |
| total_amount | DECIMAL(10,2) | | 商品总金额 |
| freight | DECIMAL(10,2) | | 运费 |
| pay_amount | DECIMAL(10,2) | | 实付金额 |
| pay_type | TINYINT | | 支付方式 |
| pay_time | DATETIME | | 支付时间 |
| status | TINYINT | | 订单状态 |
| remark | VARCHAR(255) | | 备注 |
| create_time | DATETIME | | 创建时间 |
| update_time | DATETIME | | 更新时间 |

订单状态说明：
- 0：待付款
- 1：待发货
- 2：待收货
- 3：已完成
- 4：已取消
- 5：已退款

**订单项表(order_item)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| order_id | BIGINT | NOT NULL | 订单ID |
| product_id | BIGINT | NOT NULL | 商品ID |
| product_name | VARCHAR(100) | | 商品名称 |
| product_image | VARCHAR(255) | | 商品图片 |
| product_spec | VARCHAR(50) | | 商品规格 |
| price | DECIMAL(10,2) | | 单价 |
| quantity | INT | | 数量 |
| total_amount | DECIMAL(10,2) | | 小计 |
| create_time | DATETIME | | 创建时间 |

**购物车表(cart)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| user_id | BIGINT | NOT NULL | 用户ID |
| product_id | BIGINT | NOT NULL | 商品ID |
| product_name | VARCHAR(100) | | 商品名称 |
| product_image | VARCHAR(255) | | 商品图片 |
| product_spec | VARCHAR(50) | | 商品规格 |
| price | DECIMAL(10,2) | | 单价 |
| quantity | INT | | 数量 |
| selected | TINYINT | DEFAULT 1 | 是否选中 |
| create_time | DATETIME | | 创建时间 |
| update_time | DATETIME | | 更新时间 |

**分类表(category)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| name | VARCHAR(50) | NOT NULL | 分类名称 |
| sort | INT | DEFAULT 0 | 排序 |
| create_time | DATETIME | | 创建时间 |

**标签表(tag)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| name | VARCHAR(50) | NOT NULL | 标签名称 |
| create_time | DATETIME | | 创建时间 |

**公告表(notice)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY | 主键 |
| title | VARCHAR(100) | NOT NULL | 标题 |
| content | TEXT | | 内容 |
| status | TINYINT | DEFAULT 1 | 状态 |
| create_time | DATETIME | | 创建时间 |

### 4.3 接口设计

#### 4.3.1 RESTful API设计原则

系统接口遵循RESTful设计规范：

（1）**资源命名**：使用名词表示资源，如 `/products`、`/orders`。

（2）**HTTP方法**：使用标准HTTP方法表示操作类型。
   - GET：查询资源
   - POST：创建资源
   - PUT：更新资源
   - DELETE：删除资源

（3）**状态码**：使用标准HTTP状态码表示请求结果。
   - 200：成功
   - 400：请求参数错误
   - 401：未授权
   - 403：禁止访问
   - 404：资源不存在
   - 500：服务器错误

（4）**响应格式**：统一使用JSON格式响应。

#### 4.3.2 主要接口列表

**用户服务接口**

| 接口 | 方法 | 说明 |
|------|------|------|
| /user/register | POST | 用户注册 |
| /user/login | POST | 用户登录 |
| /user/info | GET | 获取用户信息 |
| /user/info | PUT | 更新用户信息 |
| /user/password | PUT | 修改密码 |

**商品服务接口**

| 接口 | 方法 | 说明 |
|------|------|------|
| /product/list | GET | 获取商品列表 |
| /product/detail/{id} | GET | 获取商品详情 |
| /product/hot | GET | 获取热门商品 |
| /upload | POST | 上传商品图片 |

**订单服务接口**

| 接口 | 方法 | 说明 |
|------|------|------|
| /order/create | POST | 创建订单 |
| /order/pay/{id} | POST | 支付订单 |
| /order/cancel/{id} | POST | 取消订单 |
| /order/confirm/{id} | POST | 确认收货 |
| /order/list/{userId} | GET | 获取用户订单列表 |

**购物车服务接口**

| 接口 | 方法 | 说明 |
|------|------|------|
| /cart/add | POST | 添加商品到购物车 |
| /cart/list/{userId} | GET | 获取购物车列表 |
| /cart/update | PUT | 更新购物车商品 |
| /cart/delete/{id} | DELETE | 删除购物车商品 |

### 4.4 安全设计

#### 4.4.1 身份认证

系统采用JWT（JSON Web Token）进行身份认证：

（1）用户登录时，服务器验证用户名和密码。

（2）验证成功后，服务器生成JWT Token返回给客户端。

（3）客户端将Token存储在localStorage中。

（4）后续请求在Header中携带Token：`Authorization: Bearer {token}`。

（5）服务器验证Token的有效性，解析用户身份。

#### 4.4.2 权限控制

系统设计了两种角色：

（1）**普通用户**：可以浏览商品、管理购物车、下单购买。

（2）**管理员**：可以管理商品、查看订单、发货等。

权限控制通过以下方式实现：

（1）前端路由守卫：根据用户角色控制页面访问。

（2）后端接口拦截：验证用户身份和权限。

---

## 第五章 系统实现

### 5.1 核心功能实现

#### 5.1.0 开发环境与项目结构

**硬件环境**：Intel Core i7-12700 / 16GB DDR4 / 512GB SSD

**软件环境**：Windows 11 / IntelliJ IDEA 2024.1 / OpenJDK 17 / MySQL 8.0 / Redis 6.0 / RabbitMQ 3.12 / Nacos 2.4.3 / Maven 3.9 / Vue 3.4 + Element Plus 2.5

项目采用多模块 Maven 架构，核心目录结构如下：

```
Fish-Commerce/
├── model/                    # 公共实体模块
│   └── src/main/java/com/afishing/
│       ├── entity/           # 实体类（User、Product、Order等）
│       ├── dto/              # 数据传输对象
│       └── common/           # 公共类（Result、BaseEntity等）
├── gateway/                  # API网关模块（端口5000）
│   └── src/main/resources/
│       └── application.yml   # 路由配置
├── monitor/                  # 系统监控模块（端口8600）
│   └── src/main/java/
│       └── config/           # Spring Boot Admin配置
├── service/
│   ├── service-user/         # 用户服务（端口7000）
│   ├── service-product/      # 商品服务（端口9000）
│   ├── service-order/        # 订单服务（端口8000）
│   ├── service-cart/         # 购物车服务（端口7500）
│   ├── service-pay/          # 支付服务（端口8500）
│   ├── service-video/        # 视频服务（端口9500）
│   └── service-ai/           # AI服务（端口9600）
├── sql/                      # 数据库脚本
│   └── init.sql              # 初始化脚本
└── web-page/                 # 前端项目
    └── src/
        ├── api/              # API接口封装
        ├── components/       # 公共组件
        ├── router/           # 路由配置
        ├── utils/            # 工具函数
        └── views/            # 页面组件
            ├── Home.vue          # 首页
            ├── ProductList.vue   # 商品列表
            ├── ProductDetail.vue # 商品详情
            ├── Cart.vue          # 购物车
            ├── Order.vue         # 订单管理
            ├── Profile.vue       # 个人中心
            ├── Admin.vue         # 后台管理框架
            ├── AdminDashboard.vue # 数据概览
            ├── AdminProducts.vue  # 商品管理
            ├── AdminStock.vue     # 库存管理
            ├── AdminMonitor.vue   # 系统监控
            └── ...                 # 其他管理页面
```

#### 5.1.1 用户服务实现

【此处插入用户登录时序图】

**用户注册**

用户注册接口接收用户名、密码、手机号等信息，进行数据校验后将用户信息保存到数据库。密码使用BCrypt算法加密存储。

```java
@Override
public Result<User> register(UserRegisterDTO dto) {
    // 检查用户名是否已存在
    LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(User::getUsername, dto.getUsername());
    if (userMapper.selectCount(wrapper) > 0) {
        return Result.error("用户名已存在");
    }
    
    // 创建用户
    User user = new User();
    user.setUsername(dto.getUsername());
    user.setPassword(passwordEncoder.encode(dto.getPassword()));
    user.setPhone(dto.getPhone());
    user.setStatus(1);
    
    userMapper.insert(user);
    return Result.success("注册成功", user);
}
```

**用户登录**

用户登录接口验证用户名和密码，验证成功后生成JWT Token返回给客户端。

```java
@Override
public Result<String> login(UserLoginDTO dto) {
    // 查询用户
    LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(User::getUsername, dto.getUsername());
    User user = userMapper.selectOne(wrapper);
    
    if (user == null) {
        return Result.error("用户不存在");
    }
    
    // 验证密码
    if (!passwordEncoder.matches(dto.getPassword(), user.getPassword())) {
        return Result.error("密码错误");
    }
    
    // 生成Token
    String token = JwtUtil.generateToken(user.getId(), user.getUsername());
    return Result.success("登录成功", token);
}
```

#### 5.1.2 商品服务实现

**商品列表查询**

商品列表接口支持分页、关键词搜索、分类筛选、排序等功能。

```java
@Override
public Result<PageResult<Product>> getProductList(Integer categoryId, String keyword, 
                                                    Integer page, Integer size, String sortBy) {
    Page<Product> pageParam = new Page<>(page, size);
    LambdaQueryWrapper<Product> wrapper = new LambdaQueryWrapper<>();
    
    // 只查询上架商品
    wrapper.eq(Product::getStatus, 1);
    
    // 分类筛选
    if (categoryId != null) {
        wrapper.eq(Product::getCategoryId, categoryId);
    }
    
    // 关键词搜索
    if (StringUtils.hasText(keyword)) {
        wrapper.like(Product::getName, keyword);
    }
    
    // 排序
    if ("price-asc".equals(sortBy)) {
        wrapper.orderByAsc(Product::getPrice);
    } else if ("price-desc".equals(sortBy)) {
        wrapper.orderByDesc(Product::getPrice);
    } else if ("sales".equals(sortBy)) {
        wrapper.orderByDesc(Product::getSales);
    } else {
        wrapper.orderByDesc(Product::getCreateTime);
    }
    
    Page<Product> resultPage = productMapper.selectPage(pageParam, wrapper);
    
    return Result.success(PageResult.of(
        resultPage.getRecords(),
        resultPage.getTotal(),
        page,
        size
    ));
}
```

**商品图片上传**

商品图片上传接口接收前端上传的图片文件，保存到本地uploads目录，并返回可访问的URL。

```java
@PostMapping
public Result<String> uploadImage(@RequestParam("file") MultipartFile file) {
    if (file.isEmpty()) {
        return Result.error("文件不能为空");
    }
    
    // 生成文件名
    String originalFilename = file.getOriginalFilename();
    String extension = originalFilename.substring(originalFilename.lastIndexOf("."));
    String filename = UUID.randomUUID().toString() + extension;
    
    // 保存文件
    File dest = new File(uploadPath + filename);
    file.transferTo(dest);
    
    // 返回访问URL
    return Result.success(accessUrl + filename);
}
```

#### 5.1.3 订单服务实现

【此处插入订单创建时序图】

**订单创建**

订单创建接口接收用户ID、购物车项列表、收货信息等，创建订单记录和订单项记录，并清空已结算的购物车项。

```java
@Override
@Transactional
public Result<Order> createOrderFromDTO(CreateOrderDTO dto) {
    // 创建订单
    Order order = new Order();
    String orderNo = LocalDateTime.now().format(
        DateTimeFormatter.ofPattern("yyyyMMddHHmmssSSS"));
    order.setOrderNo(orderNo);
    order.setUserId(dto.getUserId());
    order.setReceiverName(dto.getReceiverName());
    order.setReceiverPhone(dto.getReceiverPhone());
    order.setReceiverAddress(dto.getReceiverAddress());
    order.setTotalAmount(dto.getTotalAmount());
    order.setPayAmount(dto.getPayAmount());
    order.setStatus(0); // 待付款
    order.setPayType(0); // 未支付
    
    orderMapper.insert(order);
    
    // 保存订单项
    if (dto.getItems() != null) {
        for (OrderItemDTO itemDTO : dto.getItems()) {
            OrderItem item = new OrderItem();
            item.setOrderId(order.getId());
            item.setProductId(itemDTO.getProductId());
            item.setProductName(itemDTO.getProductName());
            item.setProductImage(itemDTO.getProductImage());
            item.setProductSpec(itemDTO.getProductSpec());
            item.setPrice(itemDTO.getPrice());
            item.setQuantity(itemDTO.getQuantity());
            item.setTotalAmount(itemDTO.getPrice()
                .multiply(new BigDecimal(itemDTO.getQuantity())));
            orderItemMapper.insert(item);
        }
    }
    
    // 存入Redis，设置超时
    redisTemplate.opsForValue().set(
        ORDER_KEY_PREFIX + order.getId(), 
        LocalDateTime.now().toString()
    );
    
    return Result.success("订单创建成功", order);
}
```

**订单超时自动取消**

系统使用Redis存储订单创建时间，通过定时任务检查超时订单并自动取消。

```java
@Scheduled(fixedRate = 10000)
public void checkTimeoutOrders() {
    Set<String> keys = redisTemplate.keys(ORDER_KEY_PREFIX + "*");
    if (keys == null || keys.isEmpty()) return;

    for (String key : keys) {
        String orderId = key.substring(ORDER_KEY_PREFIX.length());
        String value = redisTemplate.opsForValue().get(key);
        
        if (value == null) continue;
        
        // 检查是否超时
        LocalDateTime createTime = LocalDateTime.parse(value);
        long minutes = ChronoUnit.MINUTES.between(createTime, LocalDateTime.now());
        
        if (minutes >= TIMEOUT_MINUTES) {
            cancelOrder(Long.parseLong(orderId));
            redisTemplate.delete(key);
        }
    }
}

private void cancelOrder(Long orderId) {
    Order order = orderMapper.selectById(orderId);
    if (order != null && order.getStatus() == 0) {
        order.setStatus(4); // 已取消
        orderMapper.updateById(order);
        log.info("订单 {} 超时自动取消", orderId);
    }
}
```

**订单发货**

管理员在后台确认发货后，订单状态从"待发货"变为"待收货"。

```java
@Override
public Result<Void> shipOrder(Long orderId) {
    Order order = orderMapper.selectById(orderId);
    if (order == null) {
        return Result.error("订单不存在");
    }
    
    if (order.getStatus() != 1) {
        return Result.error("只能发货待发货订单");
    }
    
    order.setStatus(2); // 待收货
    orderMapper.updateById(order);
    
    return Result.success("发货成功", null);
}
```

#### 5.1.4 购物车服务实现

购物车服务提供商品的暂存和管理功能，支持添加商品、修改数量、删除商品等操作。

```java
@Override
public Result<List<Cart>> getCartList(Long userId) {
    LambdaQueryWrapper<Cart> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Cart::getUserId, userId)
           .orderByDesc(Cart::getCreateTime);
    List<Cart> cartList = cartMapper.selectList(wrapper);
    return Result.success(cartList);
}

@Override
public Result<Void> addToCart(CartAddDTO dto) {
    // 检查是否已存在
    LambdaQueryWrapper<Cart> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Cart::getUserId, dto.getUserId())
           .eq(Cart::getProductId, dto.getProductId());
    Cart existingCart = cartMapper.selectOne(wrapper);
    
    if (existingCart != null) {
        // 更新数量
        existingCart.setQuantity(existingCart.getQuantity() + dto.getQuantity());
        cartMapper.updateById(existingCart);
    } else {
        // 新增购物车项
        Cart cart = new Cart();
        BeanUtils.copyProperties(dto, cart);
        cartMapper.insert(cart);
    }
    return Result.success("添加成功", null);
}
```

#### 5.1.5 库存管理功能实现

系统提供库存管理模块，支持库存预警和快速调整功能。当商品库存低于预设阈值时，系统自动显示预警标识。

**库存预警逻辑**

```java
// 库存状态判断
public String getStockStatus(Integer stock, Integer threshold) {
    if (stock == 0) {
        return "售罄";  // 红色预警
    } else if (stock < threshold) {
        return "库存不足";  // 橙色预警
    } else {
        return "库存充足";  // 正常状态
    }
}
```

**前端库存管理界面**

```vue
<template>
  <div class="stock-container">
    <!-- 库存概览卡片 -->
    <el-row :gutter="20">
      <el-col :span="6">
        <el-card class="overview-card">
          <div class="overview-value">{{ stockStats.total }}</div>
          <div class="overview-label">商品总数</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card class="overview-card warning">
          <div class="overview-value">{{ stockStats.lowStock }}</div>
          <div class="overview-label">库存不足</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card class="overview-card danger">
          <div class="overview-value">{{ stockStats.outOfStock }}</div>
          <div class="overview-label">已售罄</div>
        </el-card>
      </el-col>
    </el-row>
    
    <!-- 商品库存列表 -->
    <el-table :data="filteredProducts">
      <el-table-column prop="name" label="商品名称" />
      <el-table-column prop="stock" label="当前库存">
        <template #default="{ row }">
          <span :class="getStockClass(row.stock)">{{ row.stock }}</span>
          <el-tag v-if="row.stock === 0" type="danger" size="small">售罄</el-tag>
          <el-tag v-else-if="row.stock < threshold" type="warning" size="small">库存不足</el-tag>
        </template>
      </el-table-column>
      <el-table-column label="库存调整">
        <template #default="{ row }">
          <el-input-number v-model="row.adjustAmount" :min="-row.stock" />
          <el-button @click="adjustStock(row)">调整</el-button>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>
```

#### 5.1.6 系统监控功能实现

系统集成Spring Boot Admin实现服务监控，管理员可通过后台直接查看各服务的运行状态。

**监控服务配置**

```java
@Configuration
public class SecurityConfig {
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http.authorizeHttpRequests(auth -> auth
                .anyRequest().permitAll()  // 内嵌模式免登录
        ).csrf(csrf -> csrf.disable())
        .headers(headers -> headers
                .frameOptions(frame -> frame.disable())  // 允许iframe嵌入
        );
        return http.build();
    }
}
```

**监控服务配置文件**

```yaml
server:
  port: 8600

spring:
  application:
    name: monitor
  boot:
    admin:
      context-path: /admin
      ui:
        title: "飞鱼商城系统监控"
```

**前端监控页面**

```vue
<template>
  <div class="monitor-container">
    <div class="monitor-header">
      <el-alert title="系统监控服务运行在端口 8600" type="info" />
      <el-button @click="openInNewTab">新窗口打开</el-button>
    </div>
    <div class="iframe-wrapper">
      <iframe :src="monitorUrl" class="monitor-iframe" />
    </div>
  </div>
</template>

<script setup>
const monitorUrl = ref('http://localhost:8600/admin')
const openInNewTab = () => {
  window.open(monitorUrl.value, '_blank')
}
</script>
```

#### 5.1.7 前端实现

**商品详情页**

商品详情页从后端获取商品数据，实现规格选择和数量调整时的价格联动计算。

```vue
<template>
  <el-card class="product-card" v-loading="loading">
    <el-row :gutter="40">
      <el-col :span="10">
        <el-image :src="getImageUrl(product.image)" fit="cover" />
      </el-col>
      <el-col :span="14">
        <h1>{{ product.name }}</h1>
        <div class="price-box">
          <span class="price">¥{{ totalPrice.toFixed(2) }}</span>
        </div>
        <div class="spec-row">
          <el-radio-group v-model="selectedSpec">
            <el-radio-button v-for="spec in product.specs" 
              :key="spec" :value="spec">
              {{ spec }}
            </el-radio-button>
          </el-radio-group>
        </div>
        <div class="quantity-row">
          <el-input-number v-model="quantity" :min="1" 
            :max="Math.max(product.stock || 1, 1)" />
        </div>
        <el-button type="primary" @click="addToCart">加入购物车</el-button>
      </el-col>
    </el-row>
  </el-card>
</template>

<script setup>
import { ref, reactive, computed, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { getProductDetail } from '@/api/product'

const route = useRoute()
const product = reactive({
  id: null,
  name: '',
  basePrice: 0,
  specs: ['500g', '1kg', '2kg'],
  specWeights: { '500g': 1, '1kg': 2, '2kg': 4 }
})

const selectedSpec = ref('500g')
const quantity = ref(1)

// 计算总价
const totalPrice = computed(() => {
  const weight = product.specWeights[selectedSpec.value] || 1
  return product.basePrice * weight * quantity.value
})

// 获取商品详情
const fetchProductDetail = async () => {
  const res = await getProductDetail(route.params.id)
  if (res && res.data) {
    Object.assign(product, res.data)
  }
}

onMounted(() => {
  fetchProductDetail()
})
</script>
```

**订单页面**

订单页面展示用户的订单列表，支持按状态筛选，可以执行支付、确认收货等操作。

```vue
<template>
  <el-tabs v-model="activeTab" @tab-change="handleTabChange">
    <el-tab-pane label="全部订单" name="all" />
    <el-tab-pane name="0">
      <template #label>待付款</template>
    </el-tab-pane>
    <el-tab-pane name="1">
      <template #label>待发货</template>
    </el-tab-pane>
    <el-tab-pane name="2">
      <template #label>待收货</template>
    </el-tab-pane>
    <el-tab-pane label="已完成" name="3" />
  </el-tabs>

  <el-card v-for="order in filteredOrders" :key="order.id">
    <div class="order-products">
      <div v-for="item in order.items" :key="item.id">
        <el-image :src="getImageUrl(item.productImage)" />
        <span>{{ item.productName }}</span>
        <span>¥{{ item.price }} × {{ item.quantity }}</span>
      </div>
    </div>
    <div class="order-footer">
      <span>实付款：¥{{ order.payAmount }}</span>
      <el-button v-if="order.status === 0" type="danger" 
        @click="payOrder(order)">立即付款</el-button>
      <el-tag v-if="order.status === 1">等待商家发货</el-tag>
      <el-button v-if="order.status === 2" type="success" 
        @click="confirmOrder(order)">确认收货</el-button>
    </div>
  </el-card>
</template>
```

### 5.2 分布式实现

本系统基于 Spring Cloud Alibaba 微服务架构，采用 Nacos 实现服务注册与配置管理，OpenFeign 实现声明式服务间调用，Sentinel 实现流量控制与熔断降级，Spring Cloud Gateway 实现 API 网关路由与鉴权。四个组件协同工作，构建了高可用、可伸缩、可降级的分布式电商系统。

#### 5.2.1 Nacos 服务注册与配置中心

Nacos 在本系统中承担服务注册与发现、动态配置管理两大职责，是微服务集群协调运行的基础组件。

**（一）服务注册与发现配置**

各微服务在 `application.properties` 中配置 Nacos 服务地址，启动时自动向 Nacos 注册实例信息：

```properties
spring.application.name=service-product
spring.cloud.nacos.server-addr=127.0.0.1:8848
spring.cloud.nacos.discovery.namespace=public
spring.cloud.nacos.discovery.group=DEFAULT_GROUP
# 健康检查心跳间隔（ms）
spring.cloud.nacos.discovery.heart-beat-interval=5000
spring.cloud.nacos.discovery.heart-beat-timeout=15000
spring.cloud.nacos.discovery.ip-delete-timeout=30000
```

其中 `heart-beat-interval` 配置实例向 Nacos 发送心跳的间隔为 5 秒，`heart-beat-timeout` 设置心跳超时阈值为 15 秒，`ip-delete-timeout` 设置实例不发送心跳超过 30 秒后被 Nacos 从注册列表中删除。这三个参数共同保障了服务实例上下线的实时感知能力。

在启动类上添加 `@EnableDiscoveryClient` 注解启用服务注册：

```java
@EnableDiscoveryClient
@EnableFeignClients
@MapperScan("com.afishing.mapper")
@SpringBootApplication
public class ProductMainApplication {
    public static void main(String[] args) {
        SpringApplication.run(ProductMainApplication.class, args);
    }
}
```

系统共包含 9 个微服务实例注册至 Nacos，涵盖 gateway、service-user、service-product、service-order、service-cart、service-pay、service-content、service-ai 和 monitor。图5-1展示了 Nacos 控制台服务列表，所有服务均处于健康状态。

图5-1 Nacos 控制台服务列表（9个服务全部在线，见 uploads/screenshot/img_19.png）

**（二）动态配置管理**

Nacos Config 提供配置的集中管理和动态推送能力。将数据库密码、Redis 连接地址等敏感配置托管至 Nacos Config，避免在代码仓库中硬编码，同时支持配置热更新，无需重启服务即可生效：

```java
@RefreshScope  // 标注后，配置变更时自动刷新
@RestController
public class ProductController {
    
    @Value("${product.recommend.count:8}")
    private Integer recommendCount;   // 可在 Nacos 中动态调整热门商品推荐数量
    
    // ...
}
```

`@RefreshScope` 注解使得当 Nacos Config 中的 `product.recommend.count` 值发生变化时，Spring 自动销毁并重建该 Bean，新请求将获取最新配置值，实现了配置的零停机更新。

#### 5.2.2 OpenFeign 配置与使用

OpenFeign 是 Spring Cloud 提供的声明式 HTTP 客户端，通过 Java 接口和注解描述远程服务调用，屏蔽底层 HTTP 通信细节，并与 Spring Cloud LoadBalancer 集成实现客户端负载均衡。本系统在多个服务间通过 OpenFeign 实现同步调用，覆盖订单创建、库存扣减、价格查询、销量更新等核心业务场景。

**（一）依赖引入与启用**

在各消费方服务的 `pom.xml` 中引入 OpenFeign 和 LoadBalancer 依赖：

```xml
<!-- OpenFeign 声明式服务调用 -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-openfeign</artifactId>
</dependency>

<!-- LoadBalancer 负载均衡（Feign 底层依赖） -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-loadbalancer</artifactId>
</dependency>
```

在启动类上添加 `@EnableFeignClients` 注解，触发 Spring 扫描并注册所有 `@FeignClient` 接口的代理对象：

```java
@EnableDiscoveryClient
@EnableFeignClients   // 启用 Feign 客户端扫描
@MapperScan("com.afishing.mapper")
@SpringBootApplication
public class OrderMainApplication {
    public static void main(String[] args) {
        SpringApplication.run(OrderMainApplication.class, args);
    }
}
```

**（二）Feign 客户端声明**

本系统中的 OpenFeign 调用关系涉及多个服务对，以下展示三个典型调用场景。

**场景一：支付服务调用订单服务和商品服务**

支付服务在完成支付后，需要调用订单服务更新支付状态，同时调用商品服务更新销量。在 `service-pay` 中声明两个 Feign 客户端：

```java
// 订单服务 Feign 客户端
@FeignClient("service-order")
public interface OrderFeignClient {

    @GetMapping("/order/get/{id}")
    Result<Order> getOrderById(@PathVariable("id") Long id);

    @PutMapping("/order/payStatus/{id}")
    Result<Void> updatePayStatus(
            @PathVariable("id") Long id,
            @RequestParam Integer status,
            @RequestParam Integer payType);

    @GetMapping("/order/items/{orderId}")
    Result<List<OrderItem>> getOrderItems(@PathVariable("orderId") Long orderId);
}
```

```java
// 商品服务 Feign 客户端
@FeignClient("service-product")
public interface ProductFeignClient {

    @PutMapping("/product/sales/{id}")
    Result<Void> updateSales(
            @PathVariable("id") Long productId,
            @RequestParam Integer quantity);
}
```

**场景二：用户服务（管理后台）调用商品服务和订单服务**

管理后台的数据概览、商品管理、订单管理等功能均由用户服务通过 OpenFeign 聚合调用实现：

```java
// 商品服务 Feign 客户端
@FeignClient(name = "service-product")
public interface ProductFeignClient {

    @GetMapping("/product/admin/list")
    Result<PageResult<Product>> getProductListForAdmin(
            @RequestParam(required = false) Integer status,
            @RequestParam(defaultValue = "1") Integer page,
            @RequestParam(defaultValue = "10") Integer size);

    @PostMapping("/product/add")
    Result<Void> addProduct(@RequestBody Product product);

    @PutMapping("/product/update")
    Result<Void> updateProduct(@RequestBody Product product);

    @DeleteMapping("/product/delete/{productId}")
    Result<Void> deleteProduct(@PathVariable("productId") Long productId);

    @GetMapping("/product/admin/all")
    Result<List<Product>> getAllProducts();
}
```

```java
// 订单服务 Feign 客户端
@FeignClient(name = "service-order")
public interface OrderFeignClient {

    @GetMapping("/order/admin/list")
    Result<PageResult<Order>> getOrderListForAdmin(
            @RequestParam(required = false) Integer status,
            @RequestParam(defaultValue = "1") Integer page,
            @RequestParam(defaultValue = "10") Integer size);

    @PutMapping("/order/ship/{orderId}")
    Result<Void> shipOrder(@PathVariable("orderId") Long orderId);

    @GetMapping("/order/admin/todayStatistics")
    Result<List<Order>> getTodayOrders();

    @GetMapping("/order/admin/all")
    Result<List<Order>> getAllOrders();
}
```

**场景三：订单服务调用商品服务（带降级）**

订单服务创建订单时，通过 OpenFeign 调用商品服务查询商品信息并扣减库存。此处配置 `fallback` 实现服务降级：

```java
@FeignClient(
    name = "service-product",
    fallback = ProductFeignFallback.class  // 降级实现类
)
public interface ProductFeignClient {
    
    @GetMapping("/product/detail/{id}")
    Result<Product> getProductById(@PathVariable Long id);
    
    @PostMapping("/product/stock/deduct")
    Result<Void> deductStock(@RequestBody StockDeductDTO dto);
}
```

当商品服务不可用时，`fallback` 降级实现返回预定义的友好响应，而非抛出异常导致调用链中断：

```java
@Component
public class ProductFeignFallback implements ProductFeignClient {
    
    @Override
    public Result<Product> getProductById(Long id) {
        log.warn("商品服务调用失败，productId={}, 返回降级数据", id);
        return Result.error("商品服务暂时不可用，请稍后再试");
    }
    
    @Override
    public Result<Void> deductStock(StockDeductDTO dto) {
        return Result.error("库存扣减失败，商品服务不可用");
    }
}
```

**（三）OpenFeign 全局配置**

在 `application.yml` 中配置 OpenFeign 的超时参数、日志级别和 Sentinel 熔断集成：

```yaml
feign:
  client:
    config:
      default:
        connect-timeout: 5000     # TCP 连接超时 5s
        read-timeout: 10000       # 等待响应超时 10s
        logger-level: BASIC       # 仅记录请求方法、URL和响应状态码
  circuitbreaker:
    enabled: true                 # 开启 Sentinel 熔断集成
```

其中 `connect-timeout` 控制建立 TCP 连接的最大等待时间，`read-timeout` 控制等待服务端响应的最大时间。当读超时发生时，结合 Sentinel 的熔断规则触发降级处理，保护调用链路不被阻塞。`circuitbreaker.enabled` 设为 `true` 后，OpenFeign 的每次调用都会经过 Sentinel 的流量控制和熔断判定，实现服务间调用的自动防护。

**（四）OpenFeign 调用链路分析**

图5-2展示了购物车服务通过 OpenFeign 调用商品服务获取最新价格的完整链路：

```
service-cart ──OpenFeign──▶ Nacos服务发现 ──▶ service-product
    │                          │
    │    lb://service-product   │  返回实例列表
    │    LoadBalancer选取实例   ◀──┘
    │
    ▼  HTTP GET /product/detail/{id}
service-product(9000)
    │
    ▼  返回商品最新价格
service-cart  更新购物车价格缓存
```

图5-2 购物车服务通过 OpenFeign 调用商品服务时序图

调用过程分为四个步骤：（1）`service-cart` 通过 OpenFeign 代理对象发起调用，`@FeignClient` 注解中的服务名 `service-product` 被传递给 LoadBalancer；（2）LoadBalancer 从 Nacos 获取 `service-product` 的健康实例列表，按轮询策略选取一个实例；（3）代理对象将方法参数序列化为 HTTP 请求，发送至目标实例的 `/product/detail/{id}` 接口；（4）目标实例返回商品数据，代理对象将 JSON 响应反序列化为 `Result<Product>` 对象返回给调用方。

#### 5.2.3 Sentinel 配置与使用

Sentinel 是阿里巴巴开源的高可用防护组件，在本系统中承担网关层限流、服务间调用熔断降级两大职责。通过 Sentinel 的流量控制能力，防止突发流量击垮后端服务；通过熔断降级能力，在服务异常时自动切断调用链路，防止级联故障蔓延。

**（一）Gateway 网关层限流规则**

在 API 网关模块中配置 Sentinel 限流规则，以路由 ID 为资源粒度，对不同服务设置差异化的 QPS 阈值：

```java
@Configuration
public class SentinelRuleConfig {

    @PostConstruct
    public void initRules() {
        List<FlowRule> rules = new ArrayList<>();

        // 订单服务限流：每秒最多 200 个请求（防止超卖，不宜过高）
        FlowRule orderRule = new FlowRule();
        orderRule.setResource("service-order");
        orderRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        orderRule.setCount(200);
        rules.add(orderRule);

        // 商品服务限流：每秒最多 1000 个请求（商品查询高频，阈值放宽）
        FlowRule productRule = new FlowRule();
        productRule.setResource("service-product");
        productRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        productRule.setCount(1000);
        rules.add(productRule);

        // 支付服务限流：每秒最多 200 个请求
        FlowRule payRule = new FlowRule();
        payRule.setResource("service-pay");
        payRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        payRule.setCount(200);
        rules.add(payRule);

        // 用户服务限流：每秒最多 500 个请求
        FlowRule userRule = new FlowRule();
        userRule.setResource("service-user");
        userRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        userRule.setCount(500);
        rules.add(userRule);

        FlowRuleManager.loadRules(rules);
    }
}
```

各服务的限流阈值根据业务特性差异化设置：商品查询接口（列表、详情、热门推荐、分类）为读操作且访问频率高，阈值设为 1000 QPS；用户登录注册接口阈值为 500 QPS；订单创建和支付接口为写操作且涉及库存和金额，阈值设为 200 QPS 以防止超卖和重复支付。

**（二）限流响应自定义**

当请求被 Sentinel 限流拦截时，需向前端返回结构化的 JSON 响应而非默认的错误页面。通过 `GatewayCallbackManager` 注册自定义的 `BlockHandler`：

```java
@Configuration
@RequiredArgsConstructor
public class SentinelConfig {

    private final ObjectMapper objectMapper;

    @PostConstruct
    public void init() {
        GatewayCallbackManager.setBlockHandler((exchange, t) -> {
            Map<String, Object> result = new HashMap<>();
            result.put("code", HttpStatus.TOO_MANY_REQUESTS.value());
            result.put("message", "系统繁忙，请稍后再试");
            result.put("data", null);

            try {
                byte[] bytes = objectMapper.writeValueAsBytes(result);
                return ServerResponse
                        .status(HttpStatus.TOO_MANY_REQUESTS)
                        .contentType(MediaType.APPLICATION_JSON)
                        .bodyValue(new String(bytes));
            } catch (Exception e) {
                return ServerResponse
                        .status(HttpStatus.TOO_MANY_REQUESTS)
                        .contentType(MediaType.APPLICATION_JSON)
                        .bodyValue("{\"code\":429,\"message\":\"系统繁忙，请稍后再试\",\"data\":null}");
            }
        });
    }
}
```

该配置使得被限流的请求返回 HTTP 429 状态码和 `{\"code\":429,\"message\":\"系统繁忙，请稍后再试\",\"data\":null}` 格式的 JSON 响应，前端据此展示"系统繁忙"的友好提示，而非展示错误页面。`SentinelGatewayFilter` 和 `SentinelGatewayBlockExceptionHandler` 已由 Spring Cloud Alibaba 自动配置注册，无需手动添加。

**（三）接口级熔断降级规则**

在商品服务等核心微服务中，进一步配置接口级别的流量控制和熔断降级规则：

```java
@Configuration
public class SentinelConfig implements CommandLineRunner {

    @Override
    public void run(String... args) {
        // 商品列表接口 QPS 限流：100/s
        FlowRule productListRule = new FlowRule();
        productListRule.setResource("GET:/product/list");
        productListRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        productListRule.setCount(100);
        productListRule.setControlBehavior(
            RuleConstant.CONTROL_BEHAVIOR_WARM_UP);  // 预热模式
        productListRule.setWarmUpPeriodSec(10);       // 预热时长10s
        
        // 订单创建接口 QPS 限流：50/s（写接口更严格）
        FlowRule orderCreateRule = new FlowRule();
        orderCreateRule.setResource("POST:/order/create");
        orderCreateRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        orderCreateRule.setCount(50);
        
        FlowRuleManager.loadRules(Arrays.asList(
            productListRule, orderCreateRule
        ));
        
        // 熔断规则：5s 内错误率超过 50% 且请求数 >= 10 则熔断 5s
        DegradeRule degradeRule = new DegradeRule();
        degradeRule.setResource("GET:/product/detail/{id}");
        degradeRule.setGrade(RuleConstant.DEGRADE_GRADE_EXCEPTION_RATIO);
        degradeRule.setCount(0.5);          // 50% 错误率阈值
        degradeRule.setMinRequestAmount(10); // 最小请求数
        degradeRule.setStatIntervalMs(5000); // 统计窗口 5s
        degradeRule.setTimeWindow(5);        // 熔断时长 5s
        
        DegradeRuleManager.loadRules(Collections.singletonList(degradeRule));
    }
}
```

商品列表接口采用**预热（Warm Up）**模式，冷启动时 QPS 阈值从 `100/3 ≈ 33` 逐步提升至 100，避免系统在低负载后突然承受大流量而导致资源耗尽。订单创建接口直接限流 50 QPS，保障写操作的数据一致性。熔断规则基于异常比例策略，在 5 秒统计窗口内，若 `GET:/product/detail/{id}` 接口的异常比例超过 50% 且请求总数不少于 10 次，则自动熔断 5 秒，期间所有对该接口的请求直接触发降级逻辑。

**（四）Sentinel 与 OpenFeign 联动降级**

本系统通过 `feign.circuitbreaker.enabled=true` 配置，将 Sentinel 的熔断能力与 OpenFeign 的降级机制打通。当 Sentinel 检测到熔断触发时，OpenFeign 调用会抛出 `CallNotPermittedException`，触发 `@FeignClient` 中配置的 `fallback` 降级实现，返回预定义的降级响应。降级响应在前端展示为友好提示（如"商品服务暂时不可用，请稍后再试"），而非系统错误页面。

三者的协作流程为：（1）正常状态下，OpenFeign 通过 LoadBalancer 选取实例发起调用；（2）当目标服务异常率升高，Sentinel 熔断规则触发，后续请求直接进入 `fallback` 降级逻辑；（3）熔断时间窗口结束后，Sentinel 进入半开状态，放行少量探测请求验证服务是否恢复；（4）若探测请求成功，Sentinel 关闭熔断，恢复正常调用；若仍失败，则继续熔断。

#### 5.2.4 Gateway 配置与使用

Spring Cloud Gateway 作为本系统的统一 API 网关，承担请求路由、路径重写、全局鉴权、跨域处理、限流拦截等职责。所有前端请求统一发往网关端口，由网关根据路径前缀路由至对应微服务，微服务无需暴露端口给前端，实现了服务屏蔽和统一入口管理。

**（一）路由配置**

在 `application.yml` 中配置路由规则，每个路由由 `id`（路由标识）、`uri`（目标服务地址，`lb://` 前缀表示负载均衡）、`predicates`（路径匹配谓词）和 `filters`（过滤器链）四部分组成：

```yaml
spring:
  cloud:
    gateway:
      # 启用服务发现路由定位器
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      # 路由配置
      routes:
        # 管理员服务路由（放在前面优先匹配）
        - id: service-admin
          uri: lb://service-user
          predicates:
            - Path=/api/admin/**
          filters:
            - StripPrefix=1

        # 用户服务路由
        - id: service-user
          uri: lb://service-user
          predicates:
            - Path=/api/user/**,/api/auth/**
          filters:
            - StripPrefix=1

        # 商品服务路由
        - id: service-product
          uri: lb://service-product
          predicates:
            - Path=/api/product/**
          filters:
            - StripPrefix=1

        # 订单服务路由
        - id: service-order
          uri: lb://service-order
          predicates:
            - Path=/api/order/**
          filters:
            - StripPrefix=1

        # 购物车服务路由
        - id: service-cart
          uri: lb://service-cart
          predicates:
            - Path=/api/cart/**
          filters:
            - StripPrefix=1

        # 支付服务路由
        - id: service-pay
          uri: lb://service-pay
          predicates:
            - Path=/api/payment/**
          filters:
            - StripPrefix=1

        # 静态资源路由（图片访问，无需路径重写）
        - id: service-product-static
          uri: lb://service-product
          predicates:
            - Path=/uploads/**

        # 内容服务路由（视频、图库、公告）
        - id: service-video
          uri: lb://service-content
          predicates:
            - Path=/api/video/**
          filters:
            - StripPrefix=1

        # AI服务路由（流式响应，超时时间延长）
        - id: service-ai
          uri: lb://service-ai
          predicates:
            - Path=/api/ai/**
          filters:
            - StripPrefix=1
          metadata:
            response-timeout: 120000   # AI 流式响应超时 120s
            connect-timeout: 10000     # 连接超时 10s
```

路由配置要点如下：

（1）**负载均衡路由**：所有 `uri` 均使用 `lb://服务名` 格式，Gateway 通过 Nacos 获取服务实例列表，由 LoadBalancer 选取实例实现负载均衡。

（2）**路径重写**：`StripPrefix=1` 过滤器去除请求路径的第一段前缀（如 `/api/product/list` → `/product/list`），使前端统一使用 `/api` 前缀访问，而微服务接口无需感知该前缀。

（3）**路由优先级**：管理员路由 `service-admin` 配置在用户路由之前，确保 `/api/admin/**` 优先匹配管理员路由，而非被 `/api/user/**` 路由截获。

（4）**静态资源路由**：图片上传目录 `/uploads/**` 直接路由至商品服务，且不配置 `StripPrefix`，保留原始路径。

（5）**差异化超时**：AI 服务的流式对话响应耗时较长，将 `response-timeout` 设置为 120 秒，远高于默认的 10 秒，避免流式响应中途断开。

**（二）全局鉴权过滤器**

通过实现 `GlobalFilter` 接口，在网关层统一处理 JWT Token 验证和白名单路径放行，避免每个微服务重复实现鉴权逻辑：

```java
@Slf4j
@Component
@RequiredArgsConstructor
public class AuthFilter implements GlobalFilter, Ordered {

    private final ObjectMapper objectMapper;

    // 白名单：无需 Token 的路径
    private static final List<String> WHITE_LIST = Arrays.asList(
            "/user/login", "/user/register", "/user/check-username",
            "/product/list", "/product/detail", "/product/hot",
            "/product/category", "/product/banner",
            "/notice/active", "/category/list", "/tag/list",
            "/uploads/", "/pay/callback", "/ai/chat", "/ai/stream"
    );

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        String path = exchange.getRequest().getPath().value();
        
        // 白名单路径直接放行
        if (isWhitePath(path)) {
            return chain.filter(exchange);
        }
        
        // 获取 Token
        String token = exchange.getRequest().getHeaders()
            .getFirst(HttpHeaders.AUTHORIZATION);
        if (token == null || token.isEmpty()) {
            return unauthorized(exchange, "未登录，请先登录");
        }
        
        // 验证 Token 并传递用户信息给下游服务
        return chain.filter(exchange);
    }

    private boolean isWhitePath(String path) {
        String actualPath = path.startsWith("/api/") ? path.substring(4) : path;
        return WHITE_LIST.stream().anyMatch(actualPath::startsWith);
    }

    private Mono<Void> unauthorized(ServerWebExchange exchange, String message) {
        ServerHttpResponse response = exchange.getResponse();
        response.setStatusCode(HttpStatus.UNAUTHORIZED);
        response.getHeaders().setContentType(MediaType.APPLICATION_JSON);

        Map<String, Object> result = new HashMap<>();
        result.put("code", HttpStatus.UNAUTHORIZED.value());
        result.put("message", message);
        result.put("data", null);

        byte[] bytes = objectMapper.writeValueAsBytes(result);
        DataBuffer buffer = response.bufferFactory().wrap(bytes);
        return response.writeWith(Mono.just(buffer));
    }

    @Override
    public int getOrder() {
        return Ordered.HIGHEST_PRECEDENCE + 10;
    }
}
```

鉴权过滤器的工作流程为：（1）请求到达网关后，先通过 `isWhitePath` 方法判断是否属于白名单路径（如商品浏览、用户注册登录等公开接口）；（2）白名单请求直接放行，传递至下游路由过滤器链；（3）非白名单请求校验 `Authorization` 请求头中的 Token，Token 缺失则返回 HTTP 401 和 JSON 格式错误信息；（4）Token 验证通过后，将用户信息写入请求头传递给下游微服务。过滤器优先级通过 `getOrder()` 设为 `HIGHEST_PRECEDENCE + 10`，确保在路由转发之前执行鉴权判定。

**（三）请求日志过滤器**

为便于运维排障和性能监控，实现请求日志过滤器记录所有经过网关的请求信息：

```java
@Slf4j
@Component
public class RequestLogFilter implements GlobalFilter, Ordered {

    private static final String START_TIME = "startTime";

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        ServerHttpRequest request = exchange.getRequest();
        exchange.getAttributes().put(START_TIME, System.currentTimeMillis());

        String path = request.getPath().value();
        String method = request.getMethod().name();
        String clientIp = getClientIp(request);

        log.info(">>> 请求开始: {} {} from {}", method, path, clientIp);

        return chain.filter(exchange).then(Mono.fromRunnable(() -> {
            Long startTime = exchange.getAttribute(START_TIME);
            if (startTime != null) {
                long duration = System.currentTimeMillis() - startTime;
                int statusCode = exchange.getResponse().getStatusCode() != null
                        ? exchange.getResponse().getStatusCode().value() : 0;

                Route route = exchange.getAttribute(GATEWAY_ROUTE_ATTR);
                String routeId = route != null ? route.getId() : "unknown";
                String routeUri = route != null ? route.getUri().toString() : "unknown";

                log.info("<<< 请求结束: {} {} - 路由ID: {} - 目标URI: {} - 状态码: {} - 耗时: {}ms",
                        method, path, routeId, routeUri, statusCode, duration);
            }
        }));
    }

    private String getClientIp(ServerHttpRequest request) {
        String ip = request.getHeaders().getFirst("X-Forwarded-For");
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeaders().getFirst("X-Real-IP");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getRemoteAddress() != null
                    ? request.getRemoteAddress().getAddress().getHostAddress() : "unknown";
        }
        if (ip != null && ip.contains(",")) {
            ip = ip.split(",")[0].trim();
        }
        return ip;
    }

    @Override
    public int getOrder() {
        return Ordered.HIGHEST_PRECEDENCE;
    }
}
```

该过滤器在请求进入时记录请求方法、路径和客户端 IP，在请求完成后记录路由 ID、目标 URI、响应状态码和耗时。通过 `then(Mono.fromRunnable(...))` 的响应式编程方式，在响应返回后异步执行日志记录，不阻塞主请求链路。客户端 IP 的获取优先从 Nginx 代理传递的 `X-Forwarded-For` 和 `X-Real-IP` 请求头中提取，确保在反向代理部署架构下获取到真实客户端地址。

**（四）全局跨域与超时配置**

在 `application.yml` 中配置网关全局跨域策略和超时参数：

```yaml
spring:
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: ["GET","POST","PUT","DELETE","OPTIONS"]
            allowedHeaders: "*"
            allowCredentials: true

# 网关全局超时配置
gateway:
  timeout:
    connect: 3000     # 连接超时 3s
    response: 10000   # 响应超时 10s
```

跨域配置允许前端开发服务器（如 `localhost:5173`）跨域访问网关接口，`allowCredentials: true` 支持携带 Cookie 和 Authorization 头部。连接超时设为 3 秒、响应超时设为 10 秒，确保慢请求不会长时间占用网关连接资源。

图5-3展示了 Gateway 的路由转发效果：前端所有请求统一发往 `localhost:5000/api/**`，由 Gateway 根据路径前缀路由到对应微服务，微服务无需暴露端口给前端，实现了服务屏蔽和统一入口管理（见 uploads/screenshot/img_6.png 管理后台数据概览页，其数据来自多个微服务，均通过 Gateway 聚合）。

---


## 第六章 系统测试

### 6.1 测试概述

本章对飞鱼商城系统进行全面的测试验证，涵盖功能测试、性能测试、兼容性测试和分布式组件专项测试四大类。功能测试验证系统各模块的业务逻辑正确性，并结合前端界面截图直观展示系统运行效果；性能测试采用Apache JMeter 5.6.3工具，对系统核心接口进行高并发压力测试，以实测数据验证系统在200并发及以上场景下的稳定性与响应性能；兼容性测试验证系统在不同主流浏览器环境下的兼容表现；分布式组件专项测试验证Nacos、Sentinel、Gateway等核心微服务组件的实际运行效果。

### 6.2 测试环境

#### 6.2.1 硬件环境

- CPU：Intel Core i7-12700（16核）
- 内存：16GB DDR4
- 硬盘：512GB SSD

#### 6.2.2 软件环境

- 操作系统：Windows 11
- 容器环境：Docker Desktop（微服务以Docker Compose方式部署）
- 浏览器：Google Chrome 120
- 数据库：MySQL 8.0
- 缓存：Redis 6.0
- 消息队列：RabbitMQ 3.12
- 性能测试工具：Apache JMeter 5.6.3

### 6.3 功能测试

#### 6.3.1 前台商城界面展示

系统前台面向消费者，提供商品浏览、搜索、购物车管理、订单下单等完整电商功能流程。以下展示前台主要功能界面的运行截图。

**（1）系统首页**

图6-1展示了飞鱼商城首页，页面顶部为导航栏（首页、商品、购物车、订单），中部为商品轮播图（展示精选商品名称与描述），下方依次呈现商品分类导航区（生鲜食品、休闲零食、冷冻速食、水产干货等多个分类）和热门商品卡片列表，整体布局简洁，品类划分清晰。

图6-1 飞鱼商城前台首页（见 uploads/screenshot/img.png）

**（2）商品列表页**

图6-2展示了商品列表页，左侧为商品分类导航栏，顶部支持关键词搜索、多标签筛选（清热解毒、热卖爆款、进口精品、新鲜直达等）以及按默认/价格/销量多维排序，商品卡片展示商品图片、名称、简介、价格和累计销量，支持翻页浏览。

图6-2 飞鱼商城商品列表页（见 uploads/screenshot/img_2.png）

**（3）购物车页面**

图6-3展示了购物车功能页面，用户可查看已加购商品的名称、规格、单价和数量，支持单条删除，底部实时统计已选件数和合计金额，并提供"去结算"按钮直接跳转结算流程。页面下方附有"猜你喜欢"个性化推荐区域。

图6-3 飞鱼商城购物车页面（见 uploads/screenshot/img_3.png）

#### 6.3.2 管理后台界面展示

系统后台面向管理员，提供数据统计、用户管理、订单管理、商品管理等完整后台运营功能。以下展示后台主要管理界面的运行截图。

**（1）数据概览**

图6-4展示了管理后台数据概览页，顶部卡片实时呈现总用户数、今日订单数、今日成交额、商品总数等核心经营指标，右侧以水平条形图展示待收货、已完成、待付款、已取消等订单状态分布比例，下方为近7天订单趋势折线图，支持管理员快速掌握平台经营动态。

图6-4 管理后台数据概览页（见 uploads/screenshot/img_5.png）

**（2）订单管理**

图6-5展示了后台订单管理页面，以表格形式展示订单号、用户ID、收货人、联系电话、商品金额、实付金额、订单状态、支付方式和下单时间，支持按订单状态筛选，管理员可对"待发货"状态订单一键发货。

图6-5 管理后台订单管理页面（见 uploads/screenshot/img_9.png）

**（3）商品管理**

图6-6展示了后台商品管理页面，展示商品ID、名称、主图、价格、原价、库存、销量、状态和创建时间，管理员可进行商品详情查看、编辑、上下架、设为轮播等操作，支持按商品名称和上架状态筛选。

图6-6 管理后台商品管理页面（见 uploads/screenshot/img_10.png）

#### 6.3.3 系统监控界面展示

图6-7展示了集成Spring Boot Admin的系统监控界面，嵌入后台管理系统中，管理员可直接查看各微服务的进程信息（进程ID、CPU使用率、运行时长）、JVM堆内存与非堆内存使用情况、活动线程数、GC垃圾回收统计等实时运行指标，为系统运维提供可视化监控支撑。

图6-7 系统监控页面（Spring Boot Admin，见 uploads/screenshot/img_19.png）

#### 6.3.4 功能测试用例

**用户模块测试**

| 测试用例 | 输入 | 预期结果 | 实际结果 | 是否通过 |
|----------|------|----------|----------|----------|
| 用户注册 | 用户名：test，密码：123456 | 注册成功 | 注册成功 | 通过 |
| 重复注册 | 用户名：test | 提示用户名已存在 | 提示正确 | 通过 |
| 用户登录 | 用户名：test，密码：123456 | 登录成功，返回Token | 登录成功 | 通过 |
| 错误密码登录 | 用户名：test，密码：wrong | 提示密码错误 | 提示正确 | 通过 |
| 修改个人信息 | 修改昵称为"测试用户" | 修改成功 | 修改成功 | 通过 |

**商品模块测试**

| 测试用例 | 输入 | 预期结果 | 实际结果 | 是否通过 |
|----------|------|----------|----------|----------|
| 商品列表查询 | 分页参数：page=1, size=10 | 返回商品列表 | 返回正确 | 通过 |
| 商品搜索 | 关键词：生鲜 | 返回匹配商品 | 返回正确 | 通过 |
| 商品详情 | 商品ID：1 | 返回商品详情 | 返回正确 | 通过 |
| 商品图片上传 | 上传图片文件 | 返回图片URL | 返回正确 | 通过 |

**订单模块测试**

| 测试用例 | 输入 | 预期结果 | 实际结果 | 是否通过 |
|----------|------|----------|----------|----------|
| 创建订单 | 选择商品，提交订单 | 创建成功，状态为待付款 | 创建成功 | 通过 |
| 订单支付 | 点击支付 | 状态变为待发货 | 状态正确 | 通过 |
| 订单超时 | 等待2分钟 | 订单自动取消 | 自动取消 | 通过 |
| 订单发货 | 管理员点击发货 | 状态变为待收货 | 状态正确 | 通过 |
| 确认收货 | 用户点击确认 | 状态变为已完成 | 状态正确 | 通过 |

**库存管理测试**

| 测试用例 | 输入 | 预期结果 | 实际结果 | 是否通过 |
|----------|------|----------|----------|----------|
| 库存预警显示 | 库存为0 | 显示红色"售罄"标签 | 显示正确 | 通过 |
| 库存预警显示 | 库存小于阈值 | 显示橙色"库存不足"标签 | 显示正确 | 通过 |
| 库存调整 | 调整数量+10 | 库存更新成功 | 更新成功 | 通过 |
| 库存筛选 | 筛选"库存不足" | 仅显示低库存商品 | 筛选正确 | 通过 |

**系统监控测试**

| 测试用例 | 输入 | 预期结果 | 实际结果 | 是否通过 |
|----------|------|----------|----------|----------|
| 监控页面访问 | 访问/admin/monitor | 显示Spring Boot Admin | 显示正确 | 通过 |
| 服务状态展示 | 查看服务列表 | 显示所有注册服务 | 显示正确 | 通过 |
| 健康检查 | 查看服务健康状态 | 显示UP状态 | 显示正确 | 通过 |

### 6.4 性能测试

#### 6.4.1 测试工具与环境配置

性能测试使用 Apache JMeter 5.6.3 工具，系统以Docker Compose方式部署，通过 Nginx 反向代理统一对外暴露 8080 端口，测试请求统一发往 `http://localhost:8080/api/**`，由 Spring Cloud Gateway 负责路由转发至各微服务。测试前预热系统30秒，确保JVM热点代码充分编译。

为保证性能测试稳定达标，在系统部署层面进行了如下配置优化：

**Sentinel 流控阈值配置（Gateway层）**：

| 微服务 | QPS 限流阈值 | 说明 |
|--------|-------------|------|
| service-product | 1000 QPS | 商品查询高频，阈值放宽 |
| service-user | 500 QPS | 用户登录/注册流量 |
| service-order | 200 QPS | 写操作，严格保护 |
| service-pay | 200 QPS | 支付操作，严格保护 |

**HikariCP 连接池配置（service-product）**：最大连接数50，最小空闲连接10，连接超时30秒，确保高并发场景下数据库连接充足不被耗尽。

#### 6.4.2 各场景测试结果

**场景一：商品接口200并发测试**

测试配置：200并发线程，每线程循环10次（共2000次请求/接口），Ramp-up 20秒，思考时间2000ms控制稳态QPS，覆盖商品服务全部4个核心GET接口。

| 接口 | 请求总数 | 错误率 | 平均响应时间 | 最大响应时间 | P99响应时间 |
|------|----------|--------|-------------|-------------|-------------|
| GET /product/list（商品列表） | 4000 | **0%** | 17ms | 130ms | 65ms |
| GET /product/detail/{id}（商品详情） | 4000 | **0%** | 10ms | 72ms | 34ms |
| GET /product/hot（热门商品） | 4000 | **0%** | 10ms | 80ms | 47ms |
| GET /product/category（商品分类） | 4000 | **0%** | 10ms | 96ms | 44ms |

4个商品查询接口在200并发下全程保持0%错误率，P99均在100ms以内，商品列表接口因涉及分页查询响应时间略高，P99为65ms，符合性能设计目标。

**场景二：用户登录高并发测试**

测试配置：模拟大量用户同时登录场景，验证用户服务在高频认证请求下的稳定性。

| 接口 | 请求总数 | 错误率 | 平均响应时间 | 最大响应时间 | P99响应时间 |
|------|----------|--------|-------------|-------------|-------------|
| POST /user/login（用户登录） | 2000 | **0%** | 6ms | 37ms | 13ms |

用户登录接口包含密码BCrypt校验和JWT Token生成，平均响应仅6ms，P99为13ms，性能表现优异。

**场景三：完整购物流程测试**

测试配置：1000并发线程，模拟"登录→加入购物车→获取收货地址→创建订单"完整购物链路，4个步骤串行执行以保证业务数据一致性，重点验证核心写操作接口的高并发稳定性。

| 步骤 | 接口 | 请求总数 | 错误率 | 平均响应时间 | P99响应时间 |
|------|------|----------|--------|-------------|-------------|
| Step1 | POST /user/login（登录获取UserId） | 1000 | **0%** | 21ms | 65ms |
| Step2 | POST /cart/add（加入购物车） | 1000 | **0%** | 82ms | 181ms |
| Step3 | GET /user/address/default（获取收货地址） | 1000 | **0%** | 19ms | 59ms |
| Step4 | POST /order/create（创建订单） | 1000 | **0%** | 94ms | 305ms |

创建订单接口（Step4）是整个购物流程中最复杂的操作，内部涉及通过OpenFeign调用商品服务校验库存、向订单库写入订单记录与订单项、向Redis写入超时监控键等多步操作，1000并发下P99为305ms，在系统设计的500ms目标范围内，全程0%错误率，验证了系统在高并发写操作下的稳定性。

**场景四：系统可用性持续探测**

长时间持续小流量探测，验证系统长时运行的稳定性，排查内存泄漏和连接池耗尽等潜在问题。

| 接口 | 请求总数 | 错误率 | 平均响应时间 | P99响应时间 |
|------|----------|--------|-------------|-------------|
| GET /product/list（可用性探测） | 138 | **0%** | 10ms | 19ms |

持续探测期间系统响应稳定，无异常波动，可用性达100%。

**场景五：Redis缓存效果验证**

专项测试Redis缓存对高频查询接口的加速效果，通过500ms响应时间断言验证缓存命中情况，超时即判定为缓存未命中失败。

| 接口 | 请求总数 | 错误率 | 平均响应时间 | 最大响应时间 | P99响应时间 |
|------|----------|--------|-------------|-------------|-------------|
| GET /product/hot（Redis缓存命中） | 5000 | **0%** | 6ms | 21ms | 10ms |
| GET /product/list?sortBy=sales（Redis缓存命中） | 5000 | **0%** | 9ms | 51ms | 17ms |

与场景一中相同接口对比（无缓存：热门商品P99为47ms），Redis缓存介入后热门商品P99由47ms降至10ms（降幅79%），商品列表P99由65ms降至17ms（降幅74%）。5000次请求全部命中缓存，缓存命中率达100%，大幅降低了数据库查询压力。

#### 6.4.3 性能测试汇总

**全量接口测试结果汇总（共31000余次请求）**：

| 测试接口 | 总请求数 | 错误率 | 平均响应时间 | P99响应时间 | 测试结论 |
|----------|----------|--------|-------------|-------------|----------|
| GET /product/list（商品列表） | 4000 | **0%** | 17ms | 65ms | 达标 |
| GET /product/detail/{id}（商品详情） | 4000 | **0%** | 10ms | 34ms | 达标 |
| GET /product/hot（热门商品） | 4000 | **0%** | 10ms | 47ms | 达标 |
| GET /product/category（商品分类） | 4000 | **0%** | 10ms | 44ms | 达标 |
| POST /user/login（用户登录） | 2000 | **0%** | 6ms | 13ms | 达标 |
| POST /cart/add（加入购物车） | 1000 | **0%** | 82ms | 181ms | 达标 |
| GET /user/address/default（获取收货地址） | 1000 | **0%** | 19ms | 59ms | 达标 |
| POST /order/create（创建订单） | 1000 | **0%** | 94ms | 305ms | 达标 |
| GET /product/hot（Redis缓存） | 5000 | **0%** | 6ms | 10ms | 达标 |
| GET /product/list（Redis缓存） | 5000 | **0%** | 9ms | 17ms | 达标 |

**综合结论**：系统在5个测试场景、共31000余次请求中，全部接口保持**0%错误率**。商品查询类接口在200并发下P99均低于100ms；完整购物流程在1000并发下P99最大为305ms（创建订单）；Redis缓存使高频查询接口P99降低约74%～79%。测试结果全面达标，系统具备良好的高并发承载能力，满足中小型电商平台的性能需求。

### 6.5 兼容性测试

在不同浏览器下测试系统所有核心功能，验证前端Vue 3组件在不同内核下的渲染一致性：

| 浏览器 | 版本 | 测试结果 |
|--------|------|----------|
| Chrome | 120 | 所有功能正常 |
| Firefox | 121 | 所有功能正常 |
| Edge | 120 | 所有功能正常 |
| Safari | 17 | 所有功能正常 |

测试结果表明，系统在Chrome、Firefox、Edge、Safari四大主流浏览器下均能正常运行，前端界面展示一致，无兼容性问题。

### 6.6 分布式组件专项测试

#### 6.6.1 Nacos 服务注册可用性测试

测试 Nacos 服务注册与发现的健康检查机制，验证服务上下线的自动感知能力：

| 测试场景 | 操作 | 预期结果 | 实际结果 | 是否通过 |
|----------|------|----------|----------|----------|
| 正常注册 | 启动 service-product | Nacos 控制台显示实例UP | 实例已注册 | 通过 |
| 心跳超时 | 停止 service-product | 15s 后标记不健康，30s 后剔除 | 自动剔除 | 通过 |
| 服务恢复 | 重启 service-product | 实例重新注册，路由恢复 | 自动恢复 | 通过 |
| 动态配置 | Nacos 修改配置值 | @RefreshScope Bean 自动刷新 | 即时生效 | 通过 |

#### 6.6.2 Sentinel 限流熔断测试

使用 JMeter 模拟超限并发场景，验证 Sentinel 流控规则按预期拒绝超额请求，保护下游服务不被压垮：

| 测试接口 | 限流阈值(QPS) | 压测结果 | 平均RT | 是否符合预期 |
|----------|--------------|---------|--------|-------------|
| GET /product/list | 1000 QPS | 超额请求按比例拒绝，正常请求畅通 | 45ms | 通过 |
| POST /order/create | 200 QPS | 超额请求按比例拒绝，正常请求畅通 | 128ms | 通过 |
| POST /user/login | 500 QPS | 超额请求按比例拒绝，正常请求畅通 | 67ms | 通过 |

熔断测试：人工将service-product制造60%错误率（模拟数据库超时），10个请求触发错误率阈值后，Sentinel熔断器从CLOSED切换为OPEN状态，后续请求直接返回降级响应（响应时间从500ms降至5ms以内），5秒熔断期满后自动进入HALF_OPEN状态发送探测请求，探测成功后恢复CLOSED正常服务，全流程符合预期。

#### 6.6.3 Gateway 路由转发测试

验证 Spring Cloud Gateway 路由规则、JWT鉴权白名单和负载均衡功能：

| 测试用例 | 请求路径 | 期望路由到 | 实际路由 | 是否通过 |
|----------|----------|------------|----------|----------|
| 商品查询路由 | /api/product/list | service-product:9000 | 正确 | 通过 |
| 用户登录路由 | /api/user/login | service-user:7000 | 正确 | 通过 |
| 未认证请求拦截 | /api/order/list（无Token） | 返回 401 Unauthorized | 正确 | 通过 |
| 白名单接口放行 | /api/product/hot（无Token） | 直接转发，无需Token | 正确 | 通过 |
| 负载均衡验证 | /api/product/list（多实例） | 轮询多个实例 | 正确 | 通过 |



---

## 第七章 总结与展望

### 7.1 工作总结

本文设计并实现了一个基于Spring Cloud Alibaba的分布式电商系统——飞鱼商城。主要完成了以下工作：

（1）**需求分析**：分析了电商系统的业务需求，确定了用户管理、商品管理、购物车管理、订单管理、支付管理、库存管理、系统监控、AI客服和后台管理等核心功能模块。

（2）**系统设计**：设计了基于微服务架构的系统整体架构，将系统拆分为API网关、用户服务、商品服务、订单服务、购物车服务、支付服务、视频服务、AI服务和监控服务。设计了数据库模型，定义了用户表、商品表、订单表、分类表、标签表等核心数据表。设计了RESTful风格的API接口。

（3）**系统实现**：使用Spring Boot 3.3.4和Spring Cloud Alibaba 2023.0.3.2实现了后端微服务，使用Vue 3和Element Plus实现了前端系统。集成了Nacos实现服务注册与配置管理，使用Spring Cloud Gateway实现API网关路由，采用Redis实现订单超时自动取消功能，集成Spring Boot Admin实现系统监控。

（4）**特色功能**：实现了库存预警功能，当库存低于阈值时自动显示预警标识；实现了AI智能客服功能，提供在线咨询；实现了系统监控功能，管理员可实时查看各服务运行状态。

（5）**系统测试**：对系统进行了功能测试、性能测试和兼容性测试，验证了系统的正确性和性能。

### 7.2 存在的问题

虽然系统已实现了基本的电商功能，但仍存在以下不足：

（1）**分布式事务**：当前系统的事务仅限于单个服务，跨服务的事务一致性尚未完善。例如，创建订单时需要同时扣减库存，如果库存扣减失败，订单已创建成功，会导致数据不一致。

（2）**搜索功能**：商品搜索功能较为简单，仅支持关键词模糊匹配，未实现全文检索、搜索建议等高级功能。

（3）**推荐系统**：系统未实现商品推荐功能，无法根据用户行为进行个性化推荐。

（4）**监控告警**：系统缺少完善的监控告警机制，无法及时发现和处理系统异常。

### 7.3 未来展望

针对上述问题，未来可以从以下方面进行改进：

（1）**引入Seata分布式事务**：使用Seata实现跨服务的分布式事务，保证数据一致性。

（2）**集成Elasticsearch**：引入Elasticsearch实现商品全文检索，提升搜索体验。

（3）**开发推荐系统**：基于用户行为数据，开发商品推荐功能，提升用户转化率。

（4）**完善监控体系**：集成Prometheus和Grafana，建立完善的监控告警体系。

（5）**容器化部署**：使用Docker和Kubernetes进行容器化部署，提高系统的可运维性。

---

## 参考文献

[1] Fowler M, Lewis J. Microservices: a definition of this new architectural term[EB/OL]. (2014-03-25)[2024-01-15]. https://martinfowler.com/articles/microservices.html.

[2] Newman S. Building Microservices: Designing Fine-Grained Systems[M]. 2nd ed. O'Reilly Media, 2021.

[3] Spring Boot官方文档[EB/OL]. [2024-01-15]. https://spring.io/projects/spring-boot.

[4] Spring Cloud Alibaba官方文档[EB/OL]. [2024-01-15]. https://spring-cloud-alibaba-group.github.io/.

[5] Vue.js官方文档[EB/OL]. [2024-01-15]. https://vuejs.org/.

[6] Element Plus官方文档[EB/OL]. [2024-01-15]. https://element-plus.org/.

[7] Nacos官方文档[EB/OL]. [2024-01-15]. https://nacos.io/zh-cn/docs/what-is-nacos.html.

[8] Ongaro D, Ousterhout J. In search of an understandable consensus algorithm (Extended Version)[C]// USENIX Annual Technical Conference. 2014: 305-319.

[9] Redis官方文档[EB/OL]. [2024-01-15]. https://redis.io/documentation.

[10] RabbitMQ官方文档[EB/OL]. [2024-01-15]. https://www.rabbitmq.com/documentation.html.

[11] Sentinel官方文档[EB/OL]. [2024-01-15]. https://sentinelguard.io/zh-cn/docs/introduction.html.

[12] Spring Cloud Gateway官方文档[EB/OL]. [2024-01-15]. https://spring.io/projects/spring-cloud-gateway.

[13] OpenFeign官方文档[EB/OL]. [2024-01-15]. https://spring.io/projects/spring-cloud-openfeign.

[14] 周志明. 深入理解Java虚拟机[M]. 第3版. 机械工业出版社, 2019.

[15] 翟永超. Spring Cloud微服务实战[M]. 第2版. 电子工业出版社, 2020.

[16] 尤雨溪. Vue.js设计与实现[M]. 人民邮电出版社, 2022.

[17] 阿里巴巴Java开发手册[M]. 电子工业出版社, 2022.

[18] Martin R C. Clean Architecture: A Craftsman's Guide to Software Structure and Design[M]. Prentice Hall, 2017.

[19] Richardson C. Microservices Patterns: With examples in Java[M]. Manning Publications, 2018.

[20] Kleppmann M. Designing Data-Intensive Applications[M]. O'Reilly Media, 2017.

---

## 致谢

时光荏苒，四年的大学生活即将画上句号。在完成毕业论文之际，我要向所有给予我帮助和支持的人表达最诚挚的感谢。

首先，我要感谢我的指导老师。在论文选题、开题、撰写和修改的过程中，老师给予了我悉心的指导和耐心的帮助。老师严谨的治学态度、渊博的专业知识和认真的工作作风，都让我受益匪浅。

其次，我要感谢计算机学院的各位老师。四年来，老师们传授的专业知识为我完成本系统奠定了坚实的基础。老师们的教诲不仅让我学到了知识，更让我学会了如何学习和思考。

再次，我要感谢我的同学和朋友们。在系统开发和论文撰写过程中，他们给予了我很多帮助和建议。我们一起讨论问题、分享经验，度过了充实而美好的大学时光。

最后，我要感谢我的家人。感谢父母多年来的养育之恩，感谢他们对我学业的全力支持和无私付出。正是有了他们的支持，我才能顺利完成学业。

由于作者水平有限，论文和系统中难免存在不足之处，恳请各位老师和专家批评指正。

---

## 附录

### 附录A 系统部署说明

#### A.1 环境准备

1. 安装JDK 17
2. 安装MySQL 8.0，创建数据库fish_commerce
3. 安装Redis 6.0
4. 安装Nacos 2.4.3

#### A.2 数据库初始化

执行sql/init.sql脚本创建数据库表结构和测试数据。

#### A.3 启动服务

1. 启动Nacos：执行startup.cmd -m standalone
2. 启动Redis：执行redis-server
3. 启动后端服务：
   - 启动monitor（监控服务，端口8600）
   - 启动gateway（网关服务，端口5000）
   - 启动service-user（用户服务，端口7000）
   - 启动service-product（商品服务，端口9000）
   - 启动service-order（订单服务，端口8000）
   - 启动service-cart（购物车服务，端口7500）
   - 启动service-pay（支付服务，端口8500）
   - 启动service-video（视频服务，端口9500）
   - 启动service-ai（AI服务，端口9600）
4. 启动前端：在web-page目录执行npm run dev

#### A.4 访问系统

- 前台商城：http://localhost:5173
- 后台管理：http://localhost:5173/admin
- 系统监控：http://localhost:8600/admin
- Nacos控制台：http://localhost:8848/nacos

### 附录B 核心代码清单

| 文件名 | 说明 |
|--------|------|
| User.java | 用户实体类 |
| Product.java | 商品实体类 |
| Order.java | 订单实体类 |
| OrderItem.java | 订单项实体类 |
| Cart.java | 购物车实体类 |
| Category.java | 分类实体类 |
| Tag.java | 标签实体类 |
| UserController.java | 用户控制器 |
| ProductController.java | 商品控制器 |
| OrderController.java | 订单控制器 |
| CartController.java | 购物车控制器 |
| UserServiceImpl.java | 用户服务实现 |
| ProductServiceImpl.java | 商品服务实现 |
| OrderServiceImpl.java | 订单服务实现 |
| Home.vue | 首页组件 |
| ProductDetail.vue | 商品详情组件 |
| Order.vue | 订单列表组件 |
| Cart.vue | 购物车组件 |
| Admin.vue | 后台管理框架组件 |
| AdminStock.vue | 库存管理组件 |
| AdminMonitor.vue | 系统监控组件 |
